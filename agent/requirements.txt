# Dependencias del agente de vetting (Etapa 3).
# Instalar dentro del .venv del proyecto:  pip install -r agent/requirements.txt

streamlit>=1.49
matplotlib>=3.8
pandas>=2.0
numpy>=1.26

# LLM real con tool calling (opcional). El agente usa el primero con API key:
#   ANTHROPIC_API_KEY -> Claude   |   OPENAI_API_KEY / OPEN_AI_API_KEY -> OpenAI
# Sin ninguna, corre en modo mock determinista (sin costo).
anthropic>=0.40        # cliente Claude (Haiku 4.5)
openai>=1.40           # cliente OpenAI (gpt-4o-mini) para function calling
python-dotenv>=1.0     # cargar las API keys desde .env
