% ============================================================
%  PLANTILLA — Reporte Etapa 2
%  Estructura mapeada 1:1 a la rúbrica del curso (45%).
%
%  Cada sección lleva en comentario el % que pesa y los
%  criterios "Excelente" que tiene que cubrir.
%  Reemplazar los TODOs y comentarios con contenido real.
% ============================================================

\documentclass[11pt,a4paper]{article}

% ---------- Paquetes ----------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}        % Latin Modern: el font por default de Overleaf
\usepackage{microtype}      % mejor kerning y justificación
\usepackage[spanish]{babel}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{tcolorbox}
\usepackage{fancyhdr}
\usepackage{caption}
\usepackage{float}
\usepackage{listings}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black,
}

\lstset{
  basicstyle=\ttfamily\small,
  frame=single,
  breaklines=true,
  showstringspaces=false,
  extendedchars=true,
  inputencoding=utf8,
  literate=%
    {á}{{\'a}}1 {é}{{\'e}}1 {í}{{\'i}}1 {ó}{{\'o}}1 {ú}{{\'u}}1
    {Á}{{\'A}}1 {É}{{\'E}}1 {Í}{{\'I}}1 {Ó}{{\'O}}1 {Ú}{{\'U}}1
    {ñ}{{\~n}}1 {Ñ}{{\~N}}1 {ü}{{\"u}}1 {Ü}{{\"U}}1
    {¿}{{?`}}1 {¡}{{!`}}1,
}

% --- Placeholder para figuras ausentes (Overleaf sin assets locales) ---
% Uso: \safeincludegraphics[opciones]{ruta}
% Si el archivo existe -> \includegraphics normal.
% Si no -> caja gris con el path para que el documento compile igual.
\newcommand{\safeincludegraphics}[2][width=0.7\textwidth]{%
  \IfFileExists{#2}{%
    \includegraphics[#1]{#2}%
  }{%
    \fbox{\parbox[c][4cm][c]{0.7\textwidth}{\centering\ttfamily\small
      [imagen pendiente]\\\detokenize{#2}}}%
  }%
}

\begin{document}

% --- Bloque de título estilo REVTeX/AIP ---
\begin{center}
  {\LARGE\bfseries Modelos de Espacio de Estados (Mamba)\\
   para \textit{Vetting} de Exoplanetas en TESS\par}
  \vspace{1.2em}
  {\normalsize José F. Zumbado Ruiz and Jeremmy Aguilar Villanueva\par}
  \vspace{0.4em}
  {\itshape Escuela de Ingeniería en Computación, Instituto Tecnológico
   de Costa Rica, Cartago, Costa Rica\par}
  \vspace{0.3em}
  {(Reporte Etapa 2, IC8104 Inteligencia Artificial, Semestre I 2026)\par}
  \vspace{0.1em}
  {(Dated: \today)\par}
\end{center}
\vspace{1.2em}

% --- Banner de portada ---
\begin{center}
  \safeincludegraphics[width=\textwidth]{figures/banner_satellite.jpg}
  \vspace{0.3em}
  {\footnotesize\itshape Fuente: NASA.\par}
\end{center}
\vspace{1em}

\begin{abstract}
\noindent
Se evalúa empíricamente si un modelo selectivo de espacio de estados
(Mamba) puede superar a baselines clásicos en la tarea de
\textit{vetting} de TESS Objects of Interest (TOIs) sobre curvas de
luz de la misión TESS, bajo restricciones realistas de \textit{dataset}
y hardware. En términos simples, el objetivo es probar si Mamba puede
distinguir mejor entre señales reales de planetas y falsos positivos
usando la forma completa de la curva de luz, en lugar de depender
solo de datos resumidos del catálogo. El conjunto experimental contiene
1\,576 TICs etiquetados (CP / FP) con \textit{splits} estratificados
por TIC en proporción 70\,/\,15\,/\,15. Se entrena una escalera
controlada de cuatro modelos bajo el mismo \textit{split} y protocolo
de evaluación: \textit{random} estratificado, regresión logística
sobre \textit{features} del catálogo, una CNN \textit{single-branch}
inspirada en AstroNet, y Mamba sobre la vista global de la curva de
luz. Sobre el \textit{test} sellado, Mamba alcanza
$\mathrm{AUC}\text{-}\mathrm{ROC} = 0.763$, superando al CNN por
$+15.9$ puntos porcentuales bajo condiciones idénticas. Un
\textit{multi-seed sweep} de cinco semillas y su \textit{ensemble}
elevan la métrica a $0.806$ con variabilidad estabilizada. El análisis
de errores y la interpretabilidad multi-método (\textit{gradient
saliency}, \textit{integrated gradients}, \textit{occlusion}) coinciden
en que los errores remanentes corresponden a curvas con SNR baja, no a
modos sistemáticos del modelo. La reproducibilidad se garantiza con
configuraciones YAML versionadas, semillas controladas, una suite de
49 pruebas automatizadas y un protocolo de \textit{test} sellado
evaluado una sola vez por modelo.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Introducción}
\label{sec:intro}

\subsection{Motivación}
\label{subsec:motivacion}

La misión TESS (\textit{Transiting Exoplanet Survey Satellite}) de la NASA
\cite{ricker2015tess, tessMission} fue diseñada para observar a cadencia
de 2 minutos las $\sim$200\,000 estrellas enanas más brillantes del
cielo, además de tomar imágenes de campo completo (FFI) que generan
curvas adicionales para muchas otras estrellas. Cada sector observado
dura $\sim$27 días y produce series temporales del orden de
$L \approx 18\,000$ puntos. De ese flujo de fotometría, una pequeña
fracción muestra señales candidatas a tránsito
planetario; la NASA cataloga estos candidatos como TOIs
(\textit{TESS Objects of Interest}).

La Fig.~\ref{fig:concept-lightcurve-transit} ilustra la idea central
del problema: una \textbf{curva de luz} registra el brillo de una
estrella a lo largo del tiempo, y un posible tránsito aparece como
una caída localizada en esa señal. La tarea de \textit{vetting}
consiste en distinguir esas caídas reales de las causadas por ruido
instrumental, sistemas binarios u otros fenómenos astrofísicos que
producen señales parecidas pero no corresponden a planetas.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.72\textwidth]{figures/concept_lightcurve_transit.png}
\caption{Representación conceptual de un tránsito planetario en una
curva de luz. Cuando un planeta pasa frente a su estrella, bloquea
una pequeña fracción de la luz observada y produce una caída
temporal en el brillo medido. La tarea de \textit{vetting} consiste
en distinguir este tipo de señales reales de falsos positivos
causados por ruido, estrellas binarias u otros fenómenos
astrofísicos.}
\label{fig:concept-lightcurve-transit}
\end{figure}

El catálogo TOI consultado para este trabajo registra más de 7\,000
candidatos según el \textit{snapshot} utilizado (al 1 de julio de 2025
se reportaban 7\,655 TOIs, 638 con etiqueta CP y 1\,987 con etiqueta FP).
Por el costo de la revisión humana, el \textit{vetting} de TOIs se apoya
cada vez más en clasificadores automáticos.

Cada candidato del catálogo TOI tiene una \textbf{disposición}: una
etiqueta breve que indica en qué punto del proceso de validación se
encuentra. Algunas son provisionales (todavía hay que estudiar el
candidato), otras son definitivas (ya se confirmó como planeta o se
descartó). Para entrenar un clasificador supervisado el modelo
necesita ejemplos con etiqueta definitiva: solo así puede aprender a
distinguir lo que ya se sabe que es planeta de lo que ya se sabe que
no lo es. La Tabla~\ref{tab:tfopwg-dispositions} resume las
disposiciones del catálogo y cuáles se usan en este proyecto.

\begin{table}[H]
\centering
\caption{Disposiciones (\texttt{tfopwg\_disp}) del catálogo TOI y su
uso en este trabajo.}
\label{tab:tfopwg-dispositions}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{l p{0.45\textwidth} l}
\toprule
\textbf{Disposición} & \textbf{Significado} & \textbf{Uso} \\
\midrule
CP  & \textit{Confirmed Planet}: planeta confirmado por TESS &
  Etiqueta positiva ($y = 1$) \\
FP  & \textit{False Positive}: señal descartada (binaria
  eclipsante, artefacto, etc.) &
  Etiqueta negativa ($y = 0$) \\
KP  & \textit{Known Planet}: planeta confirmado por misiones
  previas (Kepler, etc.) &
  Excluido por decisión experimental \\
PC  & \textit{Planet Candidate}: candidato aún sin confirmar &
  Excluido (sin etiqueta definitiva) \\
APC & \textit{Ambiguous Planet Candidate}: candidato con
  evidencia ambigua &
  Excluido (sin etiqueta definitiva) \\
FA  & \textit{False Alarm}: artefacto instrumental &
  No considerado en este estudio \\
\bottomrule
\end{tabular}
\end{table}

El \textit{dataset} usado en este proyecto se restringe a TOIs con
etiqueta \textbf{CP} (positivo) o \textbf{FP} (negativo). KP se
excluye por decisión experimental: incluirlo mezclaría planetas
conocidos por misiones anteriores (Kepler, K2) con confirmaciones
obtenidas por la propia TESS, lo cual podría sesgar la evaluación.
PC y APC se excluyen porque no tienen etiqueta definitiva todavía.
Tras filtrar adicionalmente TICs sin cobertura fotométrica
descargable mediante \texttt{lightkurve}, se obtiene el conjunto
final de 1\,576 TICs etiquetados. 

Para situar este tamaño respecto a los baselines de la literatura,
AstroNet \cite{shallue2018} se entrenó sobre 15\,737 TCEs etiquetados
de Kepler (3\,600 PC, 9\,596 AFP y 2\,541 NTP), y ExoMiner
\cite{exominer} reporta un \textit{dataset} de trabajo de 30\,609
ejemplos sobre $\sim$34\,000 TCEs de Kepler DR25. El \textit{dataset}
de este proyecto es aproximadamente un orden de magnitud menor que el
de AstroNet y cerca de 20 veces menor que el de ExoMiner. Esta
diferencia de escala condiciona la magnitud absoluta de las métricas
reportadas y se discute al evaluar la comparación con la literatura
(Sec.\ref{sec:discussion}).

Los modelos de referencia en la literatura, como AstroNet
\cite{shallue2018} y ExoMiner \cite{exominer}, utilizan arquitecturas
profundas basadas en representaciones de curvas de luz, con componentes
convolucionales y vistas globales y locales. Este enfoque tiene dos
limitaciones documentadas. Primero, el campo receptivo de una CNN crece
de forma lineal con la profundidad y la captura de dependencias largas
en secuencias de $L=18\,000$ requiere arquitecturas profundas, costosas
de entrenar. Segundo, las arquitecturas tipo Transformer, que sí modelan
dependencias largas de forma nativa, tienen complejidad $\mathcal{O}(L^2)$
en memoria y cómputo, lo cual es prohibitivo bajo la restricción de
hardware del proyecto (RTX 3050 con 4\,GB de VRAM).

Los modelos de espacio de estados selectivos (Selective State Space Models,
SSM) propuestos por Gu y Dao \cite{gu2023mamba} ofrecen complejidad
$\mathcal{O}(L)$ con memoria constante mediante un \textit{selective scan},
y han igualado o superado a los Transformers en varias tareas de secuencia
larga. Hasta donde se revisó en la literatura del proyecto, no se
encontró una aplicación previa de Mamba al \textit{vetting} de TOIs en
TESS bajo restricciones de hardware similares. Esta brecha constituye
la motivación técnica del proyecto.

\subsection{Objetivos de la Etapa 2}
\label{subsec:objetivos}

La Etapa 2 del curso evalúa la fase de
\textit{Modelado, Entrenamiento, XAI y Evaluación}. Los criterios de la
rúbrica fijan cinco ejes de trabajo. Este reporte se organiza para
cubrir cada uno de forma explícita.

\begin{enumerate}
  \item \textbf{Baselines obligatorios.} Entregar al menos dos baselines
    razonables y una comparación clara contra el modelo principal. En este
    trabajo se construye una \textit{escalera} de cuatro modelos
    (Sec.\ref{sec:baselines}) que permite atribuir cada incremento de
    desempeño a un componente concreto de la arquitectura.
  \item \textbf{Validación y protocolo.} Adoptar un protocolo sólido con
    \textit{test} ciego, \textit{split} por TIC y prevención explícita
    de \textit{leakage} (Sec.\ref{sec:protocol}).
  \item \textbf{Optimización de hiperparámetros.} Realizar una
    búsqueda acotada y documentada de hiperparámetros, justificando
    explícitamente por qué no se ejecutó una búsqueda exhaustiva
    bajo las restricciones de VRAM y tiempo de entrenamiento
    (Sec.\ref{sec:tuning}). Por las mismas restricciones se
    sustituye la validación cruzada por una estrategia multi-semilla
    equivalente en términos de variabilidad estadística: con $K=5$ y
    un \textit{sweep} de $\sim 20$ configuraciones, la validación
    cruzada escalaría a más de 100 horas de GPU en la RTX 3050 de
    4\,GB de VRAM, lo cual es computacionalmente inviable en el
    contexto de este proyecto.
  \item \textbf{Métricas y análisis de resultados.} Reportar métricas
    apropiadas a la clase desbalanceada, curvas ROC y PR, análisis de
    errores y variabilidad seed-by-seed (Secs.~\ref{sec:results}
    y~\ref{sec:errors}).
  \item \textbf{Interpretabilidad (XAI).} Aplicar técnicas adecuadas al
    tipo de modelo y derivar hallazgos accionables (Sec.\ref{sec:xai}).
    Dado que Mamba no implementa atención en el sentido del Transformer,
    se utilizan métodos basados en gradiente y oclusión en lugar de
    mapas de atención.
\end{enumerate}

A esto se suma el eje transversal de \textbf{ingeniería y
reproducibilidad} (Sec.\ref{sec:repro}), cubierto mediante configuraciones
YAML por experimento, semillas fijas, un protocolo de \textit{test} sellado
y una suite de pruebas automatizadas (Sec.\ref{sec:tests}).

\subsection{Contribuciones}
\label{subsec:contribuciones}

Este reporte aporta lo siguiente.

\begin{enumerate}
  \item Una \textbf{comparación controlada de cuatro modelos} sobre el mismo
    \textit{split} sellado de TESS: \textit{random} estratificado, regresión
    logística sobre el catálogo TOI, CNN \textit{single-branch} inspirada
    en AstroNet \cite{shallue2018}, y Mamba con vista global como modelo
    principal.
  \item Una \textbf{exploración experimental de Mamba aplicado al
    \textit{vetting} de TOIs en TESS}. Hasta donde se revisó en la
    literatura del proyecto, no se encontró una aplicación previa bajo
    restricciones de hardware similares. El modelo se entrena en FP32
    con \textit{gradient clipping}, lo que permite procesar
    secuencias de longitud 18\,000 dentro de 4\,GB de VRAM.
  \item Un \textbf{protocolo multi-semilla} (cinco semillas para Mamba
    \textit{single-view}) que reemplaza la validación cruzada $K$-fold
    sin sacrificar la estimación de variabilidad. La sustitución se
    justifica en términos del costo estimado y se documenta en
    Sec.\ref{sec:tuning}.
  \item Un \textbf{análisis XAI multi-método} (\textit{gradient saliency},
    \textit{integrated gradients} y \textit{occlusion sensitivity}) sobre
    Mamba, con una política de selección reproducible de ocho casos
    (los dos más confidentes por cuadrante TP, TN, FN, FP).
  \item Una \textbf{infraestructura de reproducibilidad} que incluye
    configuraciones YAML versionadas, semillas controladas, una suite de
    49 pruebas automatizadas, manifiestos de entorno por corrida y un
    protocolo de \textit{test} sellado evaluado una sola vez por modelo.
\end{enumerate}

El repositorio incluye además modelos exploratorios adicionales cuyos
resultados están documentados en \texttt{paper/results/} pero quedan
fuera del alcance del presente reporte.



% ============================================================
\section{Datos y preprocesamiento}
\label{sec:data}

\subsection{Fuentes y construcción del dataset}
\label{subsec:fuentes}

El \textit{dataset} se construye a partir de dos fuentes públicas de la NASA
que se consultan programáticamente.

\begin{itemize}
  \item \textbf{NASA Exoplanet Archive: TOI Catalog}
    \cite{nasaExoplanetArchiveTOI}. Tabla pública con los TOIs
    (\textit{TESS Objects of Interest}) y su disposición científica
    (CP, FP, PC, KP, APC, FA). Se descarga vía una consulta TAP
    (\textit{Table Access Protocol}) ejecutada por
    \texttt{scripts/get\_data.py}. Del catálogo se conservan únicamente
    las columnas relevantes para el experimento: identificador
    (\texttt{tid}), disposición (\texttt{tfopwg\_disp}), y los parámetros
    de tránsito utilizados como \textit{features} por la regresión
    logística (Sec.\ref{subsec:logreg}): \texttt{pl\_orbper},
    \texttt{pl\_trandurh}, \texttt{pl\_trandep} y \texttt{st\_tmag}.
  \item \textbf{MAST Archive} \cite{mastTessProducts}. Almacén oficial
    de los archivos FITS producidos por la \textit{pipeline} SPOC
    (\textit{Science Processing Operations Center}) de TESS. Para cada
    TIC se descarga la curva \texttt{PDCSAP\_FLUX} a cadencia de 2
    minutos, que ya viene detrendeada de sistemáticos instrumentales
    por la \textit{pipeline} oficial. La descarga se realiza con la
    librería \texttt{lightkurve} \cite{lightkurve}, mediante el
    \textit{script} \texttt{scripts/download\_lightcurves.py},
    idempotente y con \textit{manifest} versionado
    (\texttt{data/splits/manifest.csv}).
\end{itemize}

La construcción del \textit{dataset} sigue una secuencia de filtros
declarada explícitamente:

\begin{enumerate}
  \item Del catálogo TOI se conservan únicamente las disposiciones
    CP (\textit{Confirmed Planet}, etiqueta positiva) y FP
    (\textit{False Positive}, etiqueta negativa). Se excluye KP por
    decisión experimental (Sec.\ref{subsec:motivacion}); se excluyen
    PC y APC por carecer de etiqueta definitiva.
  \item Se descartan TICs sin \texttt{PDCSAP\_FLUX} accesible en MAST
    o cuya descarga vía \texttt{lightkurve} falla tras los reintentos
    configurados.
  \item Se descartan TICs cuyo preprocesamiento global produce un
    tensor degenerado (por ejemplo, curva con más del 50\,\% de
    \texttt{NaN} o longitud útil menor a 5\,000 puntos).
\end{enumerate}

El conjunto resultante contiene 1\,576 TICs etiquetados. Sus particiones
\textit{train}, \textit{validation} y \textit{test} se construyen por TIC
(Sec.\ref{sec:protocol}) en proporción aproximada 70/15/15.
Tabla~\ref{tab:dataset-counts} resume los conteos.

\begin{table}[H]
\centering
\caption{Conteos por \textit{split} y clase. El \textit{split} se
realiza por TIC y de forma estratificada para preservar la proporción
CP/FP entre particiones (Sec.\ref{sec:protocol}).}
\label{tab:dataset-counts}
\begin{tabular}{lccc}
\toprule
\textit{Split} & N & CP & FP \\
\midrule
Train      & 1\,103 & 422 & 681 \\
Validation & 236    & 90  & 146 \\
Test       & 237    & 91  & 146 \\
\midrule
Total      & 1\,576 & 603 & 973 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Vista global}
\label{subsec:global-view}

La \textbf{vista global} es la representación principal del modelo
Mamba y de las CNN \textit{single-branch}. Se construye en
\texttt{scripts/preprocess\_global.py} con tres pasos:

\paragraph{1. Lectura de la señal cruda.}
Por cada TIC se concatenan los flujos \texttt{PDCSAP\_FLUX} de todos
los sectores descargados, ordenados por tiempo. Esta señal ya viene
detrendeada por SPOC y con artefactos sistemáticos removidos.

\paragraph{2. Normalización por curva individual.}
Sea $\mathbf{f} = (f_1, \dots, f_T)$ la señal cruda concatenada y
$\tilde{f} = \mathrm{median}(\mathbf{f})$ su mediana. La curva
normalizada $\mathbf{x}$ se define como
\begin{equation}
  x_t = \frac{f_t}{\tilde{f}}, \qquad t = 1, \dots, T.
\label{eq:median-norm}
\end{equation}
Intuitivamente, esta normalización hace que todas las curvas queden
centradas alrededor de $1.0$. Así, el modelo no aprende simplemente
que una estrella es más brillante que otra (información irrelevante
para detectar tránsitos), sino que se enfoca en la \textbf{forma
relativa} de las caídas de brillo. La normalización se hace siempre
con estadísticas de la propia curva, nunca con estadísticas globales
del \textit{dataset}: esta decisión evita el \textit{leakage} de
escala desde el \textit{train} al \textit{test}
(Sec.\ref{sec:protocol}).

\paragraph{3. Ajuste a longitud fija $L=18\,000$.}
Las curvas resultantes tienen longitudes variables (función del número
de sectores observados). Para que el modelo opere con tensores de forma
homogénea se aplica:
\begin{itemize}
  \item Si $T \geq L$, se recorta al primer bloque contiguo de $L$
    puntos válidos.
  \item Si $T < L$, se rellena con la mediana de la curva (es decir,
    con el valor neutro $1.0$ tras la normalización).
\end{itemize}
Los valores \texttt{NaN} restantes se interpolan linealmente sobre el
índice temporal. La salida es un tensor \texttt{float32} de forma
$(1, 18\,000)$ guardado como
\texttt{data/processed/global/<tic>.pt}. Este contrato lo verifica
\texttt{test\_dataset.py} (Sec.\ref{sec:tests}).

\paragraph{Qué modelos la consumen.}
La vista global es el \textit{input} de los modelos de aprendizaje
profundo evaluados en este reporte: la CNN \textit{single-branch}
(Sec.\ref{subsec:cnn}) y Mamba \textit{single-view}
(Sec.\ref{subsec:mamba}). No se construyen vistas adicionales sobre la
curva.

\subsection{Data augmentation}
\label{subsec:augmentation}

Para mitigar el tamaño reducido del \textit{dataset}
(Sec.\ref{subsec:motivacion}) se aplican cuatro transformaciones
estocásticas a la vista global, definidas en
\texttt{src/exoplanet/data/augment.py}. Estas se aplican
\textbf{únicamente al \textit{split} de \textit{train}}; \textit{val}
y \textit{test} se evalúan con la señal cruda preprocesada, para que
las métricas reflejen desempeño sobre datos no modificados.

\begin{table}[H]
\centering
\caption{Transformaciones de \textit{augmentation} aplicadas al
\textit{split} de \textit{train}.}
\label{tab:augmentation}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{p{0.22\textwidth} p{0.32\textwidth} p{0.38\textwidth}}
\toprule
\textbf{Transformación} & \textbf{Operación} & \textbf{Hiperparámetros del proyecto} \\
\midrule
\texttt{temporal\_shift} &
  Desplazamiento circular de la curva: $x'_t = x_{(t + s) \bmod L}$
  con $s \sim \mathcal{U}\{-S, \dots, S\}$. &
  $S = 500$ (puntos). \\
\addlinespace
\texttt{gaussian\_noise} &
  $x'_t = x_t + \varepsilon_t,\ \varepsilon_t \sim \mathcal{N}(0, \sigma^2)$. &
  $\sigma = 0.001$ (orden del ruido instrumental TESS típico). \\
\addlinespace
\texttt{time\_reverse} &
  Inversión temporal $x'_t = x_{L-t+1}$ con probabilidad $p$.
  Válida por la simetría temporal del tránsito. &
  $p = 0.5$. \\
\addlinespace
\texttt{amplitude\_scale} &
  $x'_t = a \cdot x_t,\ a \sim \mathcal{U}(a_{\min}, a_{\max})$.
  Simula diferencias de calibración. &
  $a \in [0.99, 1.01]$. \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{\texttt{temporal\_shift}: variabilidad en la posición temporal del tránsito.}
Una curva de luz no siempre tiene el tránsito en el mismo lugar dentro
de la ventana de 18\,000 puntos: depende del momento exacto en que
empezó la observación del sector. Si el modelo solo viera tránsitos
``en el centro'' de la curva, podría aprender a buscar señal solo en
esa región. El desplazamiento circular mueve la curva a izquierda o
derecha por una cantidad aleatoria, generando ejemplos donde el mismo
tránsito aparece en posiciones distintas. Esto fuerza al modelo a
volverse \textbf{invariante a la posición temporal} de la señal, una
propiedad deseable porque el momento del tránsito no debería influir
en la decisión de si es planeta o no.

\paragraph{\texttt{gaussian\_noise}: tolerancia al ruido fotométrico.}
Las curvas TESS reales contienen ruido instrumental y fotométrico:
fluctuaciones asociadas al detector, a la estadística de fotones, a
artefactos de procesamiento y a variabilidad estelar no relacionada
con el tránsito. Sumar ruido gaussiano artificial de magnitud
similar al ruido real ($\sigma = 10^{-3}$ es el orden de magnitud
típico tras la normalización por mediana) entrena al modelo para que
su decisión \textbf{no dependa de las fluctuaciones puntuales} sino
de la estructura subyacente de la curva. Sin este
\textit{augmentation}, un modelo podría sobreajustarse al patrón
exacto de ruido de las curvas de entrenamiento.

\paragraph{\texttt{time\_reverse}: aprovechar la simetría del tránsito.}
La forma de un tránsito planetario es aproximadamente simétrica en
el tiempo: el flujo decae cuando el planeta entra al disco estelar
(ingreso) y se recupera cuando sale (egreso) siguiendo una curva
espejo. Esto significa que si invertimos el tiempo de la curva, un
tránsito real sigue pareciendo un tránsito real. La transformación
explota esta propiedad para \textbf{duplicar efectivamente el número
de tránsitos disponibles} sin necesidad de descargar nuevas curvas.
La probabilidad $p = 0.5$ aplica la inversión a la mitad de los
ejemplos por época, manteniendo diversidad sin sesgar el
\textit{dataset}.

\paragraph{\texttt{amplitude\_scale}: tolerancia a diferencias de calibración.}
Aunque la normalización por mediana (Sec.\ref{subsec:global-view})
centra las curvas alrededor de $1.0$, pueden persistir pequeñas
diferencias de calibración entre estrellas o sectores. Multiplicar
toda la curva por un factor aleatorio cercano a $1$ (en $[0.99,
1.01]$, es decir, hasta un 1\,\% de cambio) simula esas diferencias y
entrena al modelo para que sea \textbf{robusto a desplazamientos
multiplicativos pequeños en la escala absoluta}, una propiedad que en
el límite ideal debería ser invariante en una tarea de clasificación
de forma del tránsito.

\paragraph{Contrato funcional común.}
Las cuatro transformaciones cumplen el mismo contrato funcional:
son puras (no mutan el tensor de entrada), preservan forma y
\texttt{dtype}, son determinísticas bajo \texttt{torch.Generator}, y
manejan correctamente los casos identidad (\textit{e.g.}, $\sigma=0$
no agrega ruido y devuelve un clon del tensor). La validación de este
contrato se hace mediante 21 pruebas unitarias en
\texttt{tests/test\_augment.py} (Sec.\ref{sec:tests}). El orden de
aplicación y los hiperparámetros se declaran por experimento en el
campo \texttt{augmentation} del YAML correspondiente.

% ============================================================
\section{Baselines}
\label{sec:baselines}

Para evaluar el aporte real de la arquitectura Mamba en este problema se
construye una \textbf{escalera de cuatro modelos} ordenada por
expresividad creciente. La idea detrás de la escalera es que cada
modelo agrega exactamente un componente respecto al anterior, de modo
que la diferencia de desempeño entre dos vecinos puede atribuirse a
ese componente concreto y no a una combinación de cambios. La
Tabla~\ref{tab:baselines-roles} resume los cuatro modelos y su rol.

\begin{table}[H]
\centering
\caption{Escalera de modelos. La columna \textit{Aporta} describe el
componente que cada modelo agrega respecto al anterior; el orden
permite atribuir ganancias o pérdidas a una variable a la vez.}
\label{tab:baselines-roles}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.27\textwidth} p{0.30\textwidth} p{0.36\textwidth}}
\toprule
\textbf{Modelo} & \textbf{Aporta respecto al anterior} & \textbf{Rol en el reporte} \\
\midrule
Random estratificado &
  Solo el \textit{prior} de clase &
  Piso mínimo (Sec.\ref{subsec:random}) \\
\addlinespace
Regresión logística (catálogo) &
  Features tabulares del TOI &
  Baseline no-DL fuerte (Sec.\ref{subsec:logreg}) \\
\addlinespace
CNN single-branch &
  Aprende representación 1D de la vista global &
  Baseline DL clásico (Sec.\ref{subsec:cnn}) \\
\addlinespace
\textbf{Mamba single-view} &
  Sustituye convolución por SSM selectivo &
  \textbf{Modelo principal de Etapa 2} (Sec.\ref{subsec:mamba}) \\
\bottomrule
\end{tabular}
\end{table}

El modelo entregable principal de Etapa 2 es Mamba \textit{single-view}.
Los tres modelos anteriores establecen una jerarquía creciente de
expresividad contra la cual se mide su aporte real.

\subsection{Random estratificado}
\label{subsec:random}

El baseline más simple no mira la curva: solo conoce la proporción
de clases en el \textit{split} de entrenamiento. Para cada muestra
emite como probabilidad constante el \textit{prior} positivo de
\textit{train}:
\begin{equation}
\hat{p}(y=1) = \frac{N_{\text{CP}}^{\text{train}}}{N_{\text{CP}}^{\text{train}} + N_{\text{FP}}^{\text{train}}} = \frac{422}{1\,103} \approx 0.383.
\label{eq:random-prior}
\end{equation}
Como esta probabilidad queda por debajo del umbral de decisión $0.5$,
el baseline clasifica todas las muestras del \textit{test} como
negativas al calcular F1, \textit{recall} y \textit{precision}. Por
eso su \textit{recall} para CP es $0$ (no recupera ningún planeta),
aunque su AUC-ROC esperado permanece en $0.5$ (la métrica no depende
del umbral, sino del ordenamiento de las probabilidades, que es
arbitrario cuando todas son iguales).

\paragraph{Para qué sirve.}
Establece el \textit{piso} contra el cual todos los modelos
posteriores deben superar. Si un modelo basado en la curva no supera
este AUC de $0.5$, es evidencia de un error de implementación o de
\textit{pipeline}, no de un problema de aprendizaje.

\subsection{Regresión logística sobre catálogo}
\label{subsec:logreg}

El segundo baseline aprovecha la información tabular ya presente en el
catálogo TOI sin tocar las curvas de luz. Por cada TIC se construye un
vector de \textit{features} $\mathbf{x} \in \mathbb{R}^{d}$ con cuatro
componentes tomadas del catálogo: el período orbital
(\texttt{pl\_orbper}), la duración del tránsito (\texttt{pl\_trandurh}),
la profundidad del tránsito (\texttt{pl\_trandep}) y la magnitud TESS
de la estrella anfitriona (\texttt{st\_tmag}). Sobre estas se ajusta
un modelo de regresión logística:
\begin{equation}
\hat{p}(y=1 \mid \mathbf{x}) = \sigma(\mathbf{w}^\top \mathbf{x} + b),
\label{eq:logreg-sigmoid}
\end{equation}
donde $\sigma(z) = 1 / (1 + e^{-z})$ es la función sigmoide.
La función $\sigma$ comprime cualquier valor real a un
número entre 0 y 1, interpretable como probabilidad.

\paragraph{Intuición.}
La regresión logística funciona como una \textbf{regla de
puntuación}: combina los datos del catálogo (período, duración,
profundidad, magnitud), produce un único número real
$\mathbf{w}^\top \mathbf{x} + b$ asignando un peso a cada
\textit{feature}, y luego transforma ese número en una probabilidad
entre 0 y 1. Si la probabilidad es alta, el modelo considera más
probable que el objeto sea un planeta confirmado; si es baja, lo
clasifica como falso positivo. Los pesos $\mathbf{w}$ son
exactamente lo que el modelo aprende durante el entrenamiento: en
qué medida cada \textit{feature} aporta evidencia a favor o en
contra del tránsito.

Los parámetros $\mathbf{w}, b$ se aprenden minimizando la pérdida de
entropía cruzada binaria con pesos por clase para corregir el
desbalance:
\begin{equation}
\mathcal{L}_{\text{BCE}}(\mathbf{w}, b) = -\frac{1}{N}\sum_{i=1}^{N}
  \Bigl[ w_+\,y_i\log \hat{p}_i + (1-y_i)\log(1-\hat{p}_i) \Bigr],
\quad w_+ = \frac{N_{\text{FP}}}{N_{\text{CP}}}.
\label{eq:bce-weighted}
\end{equation}

\paragraph{Decisión de diseño.}
Las \textit{features} usadas vienen \textbf{directamente del catálogo},
no se recalculan desde la curva (por ejemplo, no corremos un algoritmo
BLS propio para reestimar el período). Esta elección se documenta debido a que  recalcular BLS sobre 1\,576 curvas con grilla densa toma horas y el catálogo TOI ya tiene esos números
con la calidad oficial de la NASA.

\paragraph{Para qué sirve.}
Establece cuánto desempeño se puede extraer de la metadata sin tocar
la curva. Si un modelo basado en la curva no supera a la regresión
logística, significa que la curva no aporta señal adicional sobre lo
que ya está en el catálogo, lo cual sería un hallazgo relevante en
sí mismo.

\subsection{CNN single-branch (AstroNet-inspired)}
\label{subsec:cnn}

Primera arquitectura de \textit{deep learning} de la escalera. Se
inspira en AstroNet (Shallue \& Vanderburg 2018) pero opera sobre una
sola rama (la vista global), lo cual la hace comparable
directamente con Mamba \textit{single-view}.

\paragraph{Operación básica de una capa convolucional 1D.}
Una capa convolucional aplica un filtro $\mathbf{k}$ de tamaño fijo
$K$ a la curva $\mathbf{x}$, deslizándolo a lo largo del tiempo:
\begin{equation}
y_t = \sum_{i=0}^{K-1} k_i \cdot x_{t-i} + b.
\label{eq:conv1d}
\end{equation}
El filtro tiene los mismos parámetros para todas las posiciones
temporales (\textit{weight sharing}), lo que le permite detectar el
mismo patrón local (por ejemplo, un \textit{dip} de tránsito) sin
importar dónde aparezca en la curva. Apilar varias capas
convolucionales con \textit{max-pooling} entre ellas agranda el
\textit{campo receptivo}: la profundidad $D$ de capas con \textit{kernel}
$K$ y \textit{pooling} $P$ produce un campo receptivo del orden de
$K \cdot P^D$ puntos.

\paragraph{Arquitectura usada.}
La red recibe la vista global de la curva (un vector de longitud
$L = 18\,000$) y produce un único número: la probabilidad de que la
curva contenga un tránsito planetario real. Internamente la procesa
en dos fases.

\emph{Fase 1: extracción de patrones locales.}
Se aplican cuatro \textbf{bloques convolucionales} en cascada. Cada
bloque transforma la representación de la curva en una versión más
abstracta y más corta:

\begin{itemize}
  \item Una \textbf{convolución 1D}
    (Sec.\ref{eq:conv1d}) recorre la curva con un filtro de tamaño 5
    y produce una nueva señal. Si el bloque anterior produjo $C$
    señales paralelas (llamadas \textit{canales}), el siguiente
    bloque las combina y produce $C'$ canales nuevos. Los canales se
    pueden pensar como ``detectores'' especializados en distintos
    patrones (uno aprende a detectar \textit{dips}, otro detecta
    pendientes ascendentes, otro variaciones suaves de fondo, etc.).
    Estos detectores \textbf{no se programan manualmente}: la red los
    aprende ajustando sus pesos durante el entrenamiento, a partir
    de los ejemplos etiquetados del \textit{split} de \textit{train}.
    El número de canales crece con la profundidad:
    $1 \to 16 \to 32 \to 64 \to 128$. Mayor número de canales
    significa más patrones distintos que la red puede reconocer
    simultáneamente.
  \item Después de la convolución se aplica una \textbf{función de
    activación ReLU} ($\max(0, x)$): mantiene las señales positivas
    y descarta las negativas. Esto introduce no-linealidad, sin la
    cual apilar capas no aportaría más expresividad que una sola.
  \item Finalmente, una \textbf{operación de
    \textit{max-pooling}} toma la señal y la reduce a la mitad de su
    longitud quedándose con el valor máximo de cada par de puntos
    consecutivos. Esto reduce el costo de cómputo de las capas
    siguientes y obliga a la red a quedarse con las características
    más prominentes.
\end{itemize}

Después de los cuatro bloques, la representación de la curva tiene
128 canales y una longitud reducida (los \textit{max-pooling}
sucesivos la han comprimido por un factor $2^4 = 16$). En este punto
la información de la curva ya no es una serie temporal puntual sino
una representación abstracta de qué patrones detectó la red.

\emph{Fase 2: clasificación.}
La representación abstracta se \textbf{aplana} (\textit{flatten}: se
convierte la matriz $\text{canales} \times \text{longitud}$ en un
vector único) y se pasa por \textbf{dos capas totalmente conectadas}
(densas) de tamaños 64 y 1. La capa final produce un único número
real, llamado \textit{logit}. Pasando ese logit por una sigmoide
(Ec.\ref{eq:logreg-sigmoid}) se obtiene la probabilidad final
$\hat{p}(y = 1 \mid \mathbf{x}) \in [0, 1]$.

\paragraph{Tamaño del modelo.}
La red tiene aproximadamente $63\,000$ parámetros entrenables (pesos
de las convoluciones, sesgos, capas densas finales). Es un modelo
chico para los estándares actuales de \textit{deep learning}, lo
cual es deliberado: con un \textit{dataset} de $1\,576$ ejemplos
etiquetados, modelos mucho mayores tienden a memorizar
\textit{train} en lugar de aprender estructura generalizable.

\paragraph{Por qué se llama \textit{single-branch}.}
La variante de AstroNet original procesa la curva en \textbf{dos
ramas paralelas}: una rama ``global'' que mira la curva completa, y
una rama ``local'' que mira solo el tránsito alineado por
\textit{phase-folding}. Esta variante simplificada usa
\textbf{solo la rama global}, lo que la hace directamente
comparable con Mamba \textit{single-view} (que también opera solo
sobre la curva completa). El código del repositorio refleja esta
decisión: el modelo se llama \texttt{cnn\_baseline} (no
\texttt{astronet}) precisamente para no confundirlo con la
arquitectura completa original.

\paragraph{Limitación que motiva Mamba.}
Para secuencias de $L = 18\,000$ puntos, el campo receptivo de una CNN
poco profunda no abarca dependencias entre regiones distantes de la
curva (por ejemplo, dos tránsitos consecutivos separados por miles de
puntos). Capturarlas requiere o bien una CNN muy profunda (cara de
entrenar), o bien una arquitectura con dependencia explícitamente
secuencial: Transformers (de costo cuadrático) o SSM como Mamba.

\subsection{Mamba single-view}
\label{subsec:mamba}

\textbf{Modelo principal del reporte.} Mamba (Gu \& Dao 2023) pertenece
a la familia de \textit{Selective State Space Models} (SSM), una
generalización de las RNN clásicas con tres propiedades clave para
secuencias largas: (i) complejidad lineal $\mathcal{O}(L)$ en cómputo y
memoria, (ii) parámetros del estado que dependen del \textit{input}
(\textit{selective scan}), y (iii) implementación eficiente en GPU
mediante \textit{kernels} fusionados.

\paragraph{Intuición: estado oculto que recorre la secuencia.}
La manera en la que vemos el estado oculto es como un detective que va leyendo la curva de luz de izquierda a
derecha, anotando en un cuaderno
(el \textit{estado oculto} $h_t$) lo más relevante que ha visto.
En cada paso $t$ recibe el nuevo valor $x_t$ y decide qué de su
cuaderno actualizar y qué olvidar. Al final del recorrido emite un
juicio. Las RNN clásicas hacían esto pero olvidaban demasiado en
secuencias largas; Mamba mantiene un estado mucho más expresivo.

\paragraph{Ecuaciones del SSM selectivo.}
La intuición anterior se traduce matemáticamente en un \textbf{estado
oculto}: una memoria interna que se actualiza en cada punto de la
curva. La ecuación siguiente no busca ser implementada manualmente
en este reporte, sino mostrar qué tipo de operación realiza Mamba:
actualizar una memoria compacta mientras recorre una secuencia
larga. Formalmente, sea $x_t \in \mathbb{R}$ el valor de la curva
en el instante $t$ y $h_t \in \mathbb{R}^N$ el estado oculto de
dimensión $N$ (configurable por el modelo). La dinámica continua
es $h'(t) = A\,h(t) + B\,x(t)$, $y(t) = C\,h(t)$, que se discretiza
a:
\begin{equation}
h_t = \bar{A}_t\,h_{t-1} + \bar{B}_t\,x_t,
\qquad
y_t = C_t\,h_t.
\label{eq:ssm}
\end{equation}
En palabras simples, Mamba \textbf{no compara todos los puntos
contra todos los demás} como un Transformer. En cambio, recorre la
curva una sola vez y va actualizando una memoria interna. Por eso
puede trabajar con secuencias largas usando menos memoria. Lo
\textbf{selectivo} de Mamba es que las matrices
$\bar{A}_t, \bar{B}_t, C_t$ \textit{dependen del input} $x_t$
mediante proyecciones lineales aprendidas: cada paso decide qué
información dejar pasar y qué descartar en función del propio valor
observado. Esto le da a Mamba una capacidad de filtrado dinámico
que las RNN clásicas no tenían.

\paragraph{Por qué importa para $L=18\,000$.}
Comparado con un Transformer, que tiene complejidad $\mathcal{O}(L^2)$
(cada token mira a todos los demás), Mamba tiene complejidad
$\mathcal{O}(L)$ porque el estado $h_t$ es de tamaño fijo y se
actualiza paso a paso. Para $L = 18\,000$:
\begin{itemize}
  \item Transformer sin trucos: $\sim L^2 = 3.24 \times 10^8$
    operaciones de \textit{attention} por capa. Memoria requerida
    para una matriz de atención FP16: $\sim 0.6\,\text{GB}$ por capa.
    \textbf{No cabe} en 4 GB de VRAM con $L = 18\,000$ a tamaños de
    \textit{batch} útiles.
  \item Mamba: $\sim L \cdot d_{\text{model}} = 1.15 \times 10^6$
    con $d_{\text{model}} = 64$. Cabe holgadamente con
    \textit{batch size} = 16 en FP32.
\end{itemize}

\paragraph{Arquitectura usada.}
La red recibe la vista global de la curva (el mismo vector de
$L = 18\,000$ puntos que consume la CNN) y produce un único número:
la probabilidad de que la curva contenga un tránsito real. La
diferencia con la CNN está en \textbf{cómo} procesa esa entrada.

\emph{Componente principal: el bloque Mamba.}
La unidad básica del modelo es un \textbf{bloque Mamba} que
implementa internamente la dinámica selectiva de la
Ec.\ref{eq:ssm}. Recibe una secuencia de longitud $L$ donde cada
posición tiene $d_{\text{model}}$ canales, y devuelve una secuencia
de la misma forma con esos canales ya procesados a través del estado
oculto. Es la pieza que el detective de la intuición anterior usa
para leer el cuaderno y actualizarlo en cada paso.

\emph{Configuración del modelo.}
La arquitectura adoptada en \texttt{configs/mamba\_small.yaml} apila
\textbf{4 bloques Mamba} en cascada, cada uno con
$d_{\text{model}} = 64$ canales. El estado oculto interno de cada
bloque tiene dimensión $d_{\text{state}} = 16$. Apilar varios
bloques aporta capacidad expresiva análoga a apilar capas en una
red profunda: los bloques inferiores extraen patrones más locales y
los superiores combinan esos patrones en representaciones de mayor
nivel.

\emph{Por qué $d_{\text{model}} = 64$ y 4 capas.}
Esta combinación es el techo que cabe en la RTX 3050 de 4\,GB de
VRAM con \textit{batch size} 16, $L = 18\,000$ y FP32.
Configuraciones más grandes ($d_{\text{model}} = 96$ con 4 capas, o
$d_{\text{model}} = 64$ con 6 capas) producen \textit{out-of-memory}
durante el primer paso del entrenamiento. La justificación detallada
de esta elección está en Sec.\ref{subsec:config-search}.

\emph{Cabeza de clasificación.}
Después de los 4 bloques Mamba, la secuencia procesada de
$(64 \text{ canales}, 18\,000 \text{ pasos})$ se reduce a un único
vector de 64 dimensiones tomando el \textbf{promedio sobre el eje
temporal} (\textit{average pooling}). Esta operación captura el
``resumen'' que el detective tiene al final de su recorrido por la
curva. Ese vector pasa por una capa totalmente conectada lineal de
64 a 1, que produce el \textit{logit} final. Pasando el logit por
una sigmoide se obtiene la probabilidad de tránsito
$\hat{p}(y = 1 \mid \mathbf{x}) \in [0, 1]$.

\paragraph{Tamaño del modelo.}
El modelo tiene \textbf{131\,393 parámetros entrenables}, del orden
del doble que el CNN \textit{single-branch} ($\sim 63\,000$
parámetros). La diferencia de tamaño es relevante para la
comparación: la ventaja de Mamba sobre el CNN reportada en
Sec.\ref{sec:results} ($+15.9$ puntos porcentuales de AUC-ROC) es
de magnitud claramente superior a lo que podría atribuirse al
simple incremento de capacidad paramétrica.

\paragraph{Detalles de entrenamiento.}
Entrenado en \textbf{FP32} (precisión completa) con
\textit{gradient clipping} $\|g\| \leq 1$ para evitar inestabilidades
numéricas conocidas en SSM sobre secuencias largas. Optimizador
AdamW con $\text{lr} = 1.5\!\times\!10^{-3}$, \textit{weight decay}
$10^{-4}$, y \textit{cosine annealing} del LR hasta $10^{-6}$ en
las últimas épocas. Máximo de 50 épocas con \textit{early stopping}
sobre val\_auc con paciencia 10. Las decisiones de hiperparámetros
se justifican en Sec.\ref{subsec:config-search}.

% ============================================================
\section{Protocolo experimental}
\label{sec:protocol}

Una arquitectura por sí sola no decide si los resultados de un estudio
son creíbles. Lo que los hace creíbles es el \textbf{protocolo}: cómo
se separan los datos, qué se mide sobre cada partición, y qué cuidados
se toman para que las métricas reportadas reflejen desempeño real y
no artefactos del procedimiento. Esta sección documenta las cuatro
decisiones que forman el esqueleto del protocolo usado en todo el
reporte.

\subsection{Splits por TIC ID}
\label{subsec:splits}

TESS no observa el cielo completo de una sola vez. En su lugar lo
divide en regiones llamadas \textbf{sectores}, cada una observada
durante aproximadamente 27 días. La Fig.~\ref{fig:tess-sectors}
muestra cómo estos sectores se distribuyen sobre la esfera celeste.
Como consecuencia del diseño de la misión, una misma estrella puede
ser observada en varios sectores consecutivos, especialmente las
estrellas cercanas a los polos eclípticos donde los sectores se
solapan.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.62\textwidth]{figures/tess_sectors.jpg}
\caption{Cobertura observacional de TESS. La misión divide el cielo
en sectores observados durante $\sim$27 días cada uno. Una misma
estrella puede aparecer en varios sectores, lo cual implica que cada
\texttt{TIC ID} puede tener múltiples curvas de luz asociadas. Fuente:
NASA/MIT.}
\label{fig:tess-sectors}
\end{figure}

Cuando una estrella es observada por TESS en varios sectores, la
NASA publica una curva de luz por cada sector. Todas esas curvas
comparten el mismo identificador, el \texttt{TIC ID}. Si al armar
los conjuntos de \textit{train}, \textit{validation} y \textit{test}
las particiones se hicieran al nivel de \textbf{curva} (cada
\texttt{.fits} a un \textit{split} al azar), un sector de una
estrella podría caer en \textit{train} y otro sector de la misma
estrella en \textit{test}. El modelo, durante el entrenamiento,
aprendería características propias de esa estrella (su ruido
instrumental específico, sus pulsaciones intrínsecas, su
variabilidad estelar) y al evaluarse en \textit{test} las
reconocería trivialmente. El resultado serían métricas infladas que
no reflejan capacidad de generalización a estrellas nuevas. Este es
el \textit{leakage} más frecuente en este dominio.

La regla adoptada es estricta: \textbf{el \textit{split} se hace por
\texttt{TIC ID}}, no por curva ni por sector. Todas las curvas
asociadas a un mismo TIC pertenecen al mismo \textit{split}. Una
estrella no puede aparecer en más de una partición.

\paragraph{Tamaños y estratificación.}
La proporción adoptada es la convencional 70 / 15 / 15. El \textit{split}
es \textbf{estratificado por clase}: se preserva la proporción CP/FP en
cada partición. La Tabla~\ref{tab:dataset-counts} de
Sec.\ref{subsec:fuentes} reporta los conteos exactos; las tres
particiones tienen aproximadamente 38\,\% de CP, consistente con la
proporción del conjunto completo. Los archivos
\texttt{data/splits/\{train,val,test\}\_tics.csv} están versionados en
el repositorio para que cualquier reproducción use exactamente los
mismos TICs.

\subsection{Test sellado}
\label{subsec:test-sellado}

El conjunto de \textit{test} se trata como sellado: \textbf{nunca se
usa para tomar decisiones durante el desarrollo}, ni para elegir
hiperparámetros, ni para hacer \textit{early stopping}, ni para
comparar variantes de arquitectura. Todo eso se hace mirando
\textit{validation}. El \textit{test} se evalúa una sola vez por
modelo, al final, para reportar la métrica del modelo entregado.

\paragraph{Por qué importa.}
Tocar el \textit{test} durante el desarrollo es una forma sutil de
sobreajuste. Aunque nunca se entrene sobre él, si se eligen
hiperparámetros viendo cuál da mejor número en \textit{test}, el
investigador efectivamente ajusta esos hiperparámetros a ese
conjunto, y la métrica final ya no estima desempeño sobre datos
nuevos: estima desempeño sobre el \textit{test} específico, que pasó
a ser parte del proceso de selección. La rúbrica del curso menciona
explícitamente este error como criterio de calificación menor.

\paragraph{Materialización operativa.}
Cada modelo entrenado se evalúa con \texttt{scripts/evaluate.py
--run <dir> --split test}, comando que solo se ejecuta una vez por
\textit{run} al terminar el desarrollo. Los resultados se guardan en
\texttt{<run\_dir>/eval\_test/} (métricas, predicciones por TIC,
curvas y gráficos) y se versionan en el repositorio. Los
\textit{scripts} \texttt{scripts/wsl2/eval\_mamba\_test\_all.sh}
documentan el conjunto de \textit{runs} cuyo \textit{test} sí fue
abierto.

\subsection{Multi-seed como sustituto de K-fold}
\label{subsec:multi-seed}

La validación cruzada \textit{K-fold} es el estándar para estimar la
variabilidad de un modelo: el \textit{dataset} se divide en $K$ partes,
se entrena $K$ veces (dejando una parte como \textit{validation} cada
vez) y se reporta la media y desviación de las métricas. Es robusta
pero cara: con $K=5$ hay que entrenar el modelo cinco veces.

En este proyecto, $K$-fold sobre Mamba es operacionalmente inviable
en el hardware disponible. Cada entrenamiento de Mamba toma
aproximadamente una hora en una RTX 3050 de 4\,GB de VRAM; un
\textit{sweep} de 20 configuraciones con $K=5$ implicaría
$\approx 100$ horas de GPU continuas, fuera del presupuesto de
cómputo de la asignatura.

\paragraph{Estrategia adoptada.}
Para estimar la variabilidad del modelo principal se usa
\textbf{multi-seed sobre la arquitectura \textit{locked}}. Mamba se
entrena cinco veces con semillas distintas
($\{42, 123, 456, 789, 2024\}$) manteniendo idénticos los datos, la
arquitectura y los hiperparámetros. La media y desviación estándar
sobre estas cinco semillas estiman cuánto cambia el desempeño debido
únicamente a la inicialización aleatoria y al orden de muestras en
\textit{train}.

\paragraph{Justificación estadística.}
Multi-seed y $K$-fold miden cosas distintas pero relacionadas: $K$-fold
estima la sensibilidad al \textit{split} de datos, mientras que
multi-seed estima la sensibilidad a la aleatoriedad del entrenamiento.
Para este reporte la segunda es la fuente dominante de variabilidad
(el \textit{split} ya es estratificado y fijo), por lo que multi-seed
captura lo que el rubric pide bajo \textit{``variabilidad''} sin
incurrir en el costo de $K$-fold. El \textit{ensemble} de las cinco
semillas, además, es la predicción final reportada en
Tabla~\ref{tab:main-results}.

\subsection{Prevención de \textit{leakage}}
\label{subsec:leakage-prevention}

Aparte del \textit{split} por TIC ya descrito, el protocolo aplica
otras dos medidas para evitar que información de \textit{train} o
\textit{test} se contamine entre sí:

\begin{itemize}
  \item \textbf{Normalización por curva individual.} Cada curva se
    divide por su propia mediana (Sec.\ref{subsec:global-view}), no
    por estadísticas globales del \textit{dataset}. Usar la mediana
    o desviación estándar calculadas sobre todo el \textit{train}
    introduciría una pequeña dependencia entre las curvas, y al
    aplicarse al \textit{test} arrastraría un sesgo de escala. La
    normalización por curva individual elimina este efecto al costo
    despreciable de calcular una mediana extra por TIC.
  \item \textbf{\textit{Augmentation} solo en \textit{train}.}
    Las cuatro transformaciones de la Sec.\ref{subsec:augmentation}
    se aplican únicamente en \textit{train}. Las particiones
    \textit{val} y \textit{test} reciben la señal cruda preprocesada,
    sin modificación estocástica. Esto garantiza que las métricas
    reflejan desempeño sobre datos reales y no sobre variantes
    artificiales generadas por el \textit{pipeline}.
\end{itemize}

Cualquier \textit{leakage} adicional (por ejemplo, recalcular
\textit{features} usando estadísticas globales o aplicar transformaciones
post-hoc al \textit{test}) está cubierto por el contrato funcional
verificado en \texttt{tests/test\_dataset.py} y
\texttt{tests/test\_augment.py} (Sec.\ref{sec:tests}).

% ============================================================
\section{Optimización de hiperparámetros}
\label{sec:tuning}

Los hiperparámetros son los \textbf{parámetros que no se aprenden} con
el optimizador: tasa de aprendizaje, tamaño de \textit{batch},
profundidad del modelo, fuerza de regularización, etc. Hay que
elegirlos antes de entrenar. Una mala elección no se compensa con más
datos ni más épocas; una buena elección puede diferenciar un modelo
que aprende de uno que diverge. Esta sección describe la estrategia
de selección usada y la justifica frente al estándar más estricto
(\textit{grid search} u Optuna sobre validación cruzada), que en este
proyecto no es viable.

\subsection{Sanity overfit por modelo}
\label{subsec:sanity}

Antes de gastar horas de GPU entrenando con el \textit{dataset}
completo, cada arquitectura nueva pasa por una prueba de cordura
(\textit{sanity check}): \textbf{¿el modelo es capaz de extraer alguna
señal de un subconjunto chico de datos cuando no hay regularización
que lo limite?}

El procedimiento es directo: se selecciona un subconjunto fijo de 64
ejemplos del \textit{train}, se apaga la regularización
(\textit{dropout} = 0, \textit{weight decay} = 0), se evalúa sobre
el mismo subconjunto (\textit{val} = \textit{train}) y se entrena por
varias decenas de épocas. Un modelo sano debe mostrar una curva de
$\textit{val\_auc}$ monótonamente creciente que supere ampliamente el
piso aleatorio ($0.5$); idealmente alcanza $1.0$ en pocas decenas de
épocas. Si \textit{val\_auc} se queda cerca de $0.5$ o no mejora, hay
un problema estructural (gradientes que no fluyen, normalización mal
puesta, error en el \textit{loader}, \textit{etc.}), y conviene
arreglarlo antes de invertir cómputo en entrenamientos largos.

\begin{table}[H]
\centering
\caption{Resultados de \textit{sanity overfit} (subset $N = 64$,
\textit{val} $=$ \textit{train}, sin \textit{dropout} ni
\textit{weight decay}). Para descartar que el bajo \textit{val\_auc}
del CNN se debiera a una configuración de entrenamiento subóptima, se
ejecuta una segunda corrida (\textit{v2}) usando la misma estrategia
con la que Mamba sí memoriza: 120 épocas con \textit{cosine annealing}
$3\!\times\!10^{-3} \to 10^{-6}$. La columna \textit{Resultado}
interpreta la curva de \textit{val\_auc}: \checkmark{} indica que la
curva sube monótonamente por encima del piso aleatorio y descarta un
error estructural en el \textit{pipeline}.}
\label{tab:sanity}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{} p{0.30\textwidth} p{0.30\textwidth} c c c @{}}
\toprule
Modelo & Configuración \textit{sanity} & Mejor val\_auc & Epoch del mejor & Resultado \\
\midrule
CNN \textit{single-branch}\par\textsc{(Sec.\ref{subsec:cnn})}
  & 30 ep, LR fija $10^{-3}$
  & 0.693 & 26 / 30  & \checkmark \\
CNN \textit{single-branch} (\textit{v2})
  & 120 ep, cosine $3\!\times\!10^{-3} \to 10^{-6}$
  & 0.761 & 112 / 120 & \checkmark \\
Mamba \textit{single-view}\par\textsc{(Sec.\ref{subsec:mamba})}
  & 120 ep, cosine $3\!\times\!10^{-3} \to 10^{-6}$
  & \textbf{1.000} & 78 / 120 & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

Los tres ensayos pasan el \textit{sanity} en el sentido estricto: las
tres curvas suben monótonamente por encima del piso aleatorio ($0.5$),
lo cual descarta errores en el \textit{pipeline} de datos
(lectura, normalización, gradientes). Pero el contraste cuantitativo
entre arquitecturas es revelador. Mamba memoriza completamente los
64 ejemplos al alcanzar $\textit{val\_auc} = 1.000$ en la época 78
con el \textit{schedule} de \textit{cosine annealing}; las épocas
restantes consolidan la memorización. El CNN
\textit{single-branch}, en cambio, no logra memorización completa
\textbf{ni siquiera con exactamente la misma estrategia de
entrenamiento}: su mejor \textit{val\_auc} se queda en $0.761$
después de 120 épocas y nunca cruza $0.80$.

\paragraph{Lectura del hallazgo.}
La diferencia entre Mamba ($1.000$) y CNN \textit{v2} ($0.761$) bajo
configuración idéntica no es atribuible a tuning de hiperparámetros:
es una diferencia de \textbf{capacidad de la arquitectura} para
modelar secuencias de longitud $L = 18\,000$ con campo receptivo
local. El CNN \textit{single-branch} simplemente no puede memorizar
64 curvas de luz completas con cuatro bloques convolucionales y
\textit{kernels} de tamaño 5, incluso quitándole toda regularización
y dándole 120 épocas. Este resultado del \textit{sanity}
\textbf{anticipa exactamente} lo observado en el \textit{test} sellado
(Sec.\ref{sec:results}, Tabla~\ref{tab:main-results}): CNN obtiene
AUC $0.604$ frente a $0.763$ de Mamba. La brecha existe ya en la
tarea trivial de memorización, así que no es plausible cerrarla solo
con mejor selección de hiperparámetros sobre la CNN. Este es el
papel metodológico del \textit{sanity overfit}: distingue, sin
ambigüedad, entre problemas de aprendizaje (resolubles con tuning)
y problemas de capacidad de arquitectura (que requieren cambiar el
modelo).

\subsection{Configuración del modelo principal}
\label{subsec:config-search}

La configuración del modelo Mamba se determinó por una combinación de
restricciones de hardware, hallazgos del \textit{sanity overfit} de
la sección anterior, y prácticas estándar de la literatura sobre SSM
para secuencias largas. No se realizó un \textit{grid search}
exhaustivo ni una búsqueda bayesiana, pero sí una \textbf{búsqueda
acotada y documentada} sobre las variables que eran viables bajo la
restricción de VRAM; el costo computacional de una búsqueda
exhaustiva se justifica en la Sec.\ref{subsec:why-no-grid}. Las
decisiones efectivas fueron las siguientes.

\begin{itemize}
  \item \textbf{Dimensión del estado oculto $d_{\text{model}} = 64$.}
    Se eligió por debajo del techo de VRAM disponible. Valores
    significativamente mayores ($d_{\text{model}} = 96$ con $L =
    18\,000$ y 4 capas) provocan \textit{out-of-memory} en la RTX
    3050 de 4\,GB durante el \textit{forward} con \textit{batch
    size} 16.
  \item \textbf{Número de capas $n_{\text{layers}} = 4$.}
    Profundidad balanceada entre capacidad expresiva y costo de
    cómputo. Profundidades mayores ($n_{\text{layers}} = 6$) saturan
    la VRAM aun manteniendo $d_{\text{model}} = 64$.
  \item \textbf{Tasa de aprendizaje $\text{lr} = 1.5\!\times\!10^{-3}$
    con \textit{cosine annealing} hacia $10^{-6}$.} Esta tasa se
    ajustó según el comportamiento del \textit{sanity overfit}: el
    \textit{sanity} mostró que $3\!\times\!10^{-3}$ aprende pero
    introduce oscilaciones, mientras que $5\!\times\!10^{-4}$
    converge demasiado lento. $1.5\!\times\!10^{-3}$ es el
    \textit{sweet spot} conservador documentado en
    \texttt{configs/mamba\_small.yaml}.
  \item \textbf{\textit{Batch size} $= 16$.} El máximo que cabe en
    4\,GB de VRAM con $L = 18\,000$ en FP32 para esta arquitectura.
  \item \textbf{Precisión y estabilidad numérica.} Se fijó FP32 para
    obtener una línea base estable antes de considerar FP16. Se
    aplica \textit{gradient clipping} $\|g\| \leq 1$ por la
    propensión de los SSM a producir gradientes grandes en
    secuencias largas.
  \item \textbf{Optimizador AdamW con \textit{weight decay}
    $10^{-4}$.} AdamW es la elección estándar para arquitecturas
    Mamba en la literatura. El \textit{weight decay} pequeño aporta
    regularización mínima sin frenar la convergencia.
\end{itemize}

La configuración resultante (\texttt{configs/mamba\_small.yaml}) se
\textbf{congeló} (\textit{locked}) antes de pasar a la fase de
\textit{multi-seed sweep}.

\paragraph{Por qué se congela la configuración.}
Una vez elegida la configuración del modelo, ya no se cambia. Cualquier
ajuste posterior implicaría volver a mirar \textit{validation}, lo que
acumula sesgo de selección. Las cinco semillas del \textit{sweep}
siguiente entrenan \textbf{exactamente la misma arquitectura} con
\textbf{exactamente los mismos hiperparámetros}: lo único que varía
es la inicialización aleatoria de los pesos y el orden de los lotes.

\subsection{Sweep multi-seed sobre la arquitectura \textit{locked}}
\label{subsec:multi-seed-sweep}

Con la configuración congelada se entrenan cinco \textit{runs}
independientes, uno por cada semilla $\{42, 123, 456, 789, 2024\}$.
La Tabla~\ref{tab:seed-sweep} reporta el \texttt{val\_auc} alcanzado
por cada \textit{run}, su media y desviación estándar. El \textit{run}
original \texttt{mamba\_small\_locked} (entrenado durante la búsqueda
de configuración con \textit{seed} por defecto) se conserva como
\textit{pointer} histórico para trazabilidad pero no se incluye en la
agregación estadística.

\begin{table}[H]
\centering
\caption{Variabilidad por semilla del Mamba \textit{single-view} sobre
la arquitectura \textit{locked}. El \textit{ensemble} se construye
promediando las probabilidades $\hat{p}_i$ de las cinco semillas
(Sec.\ref{sec:results}).}
\label{tab:seed-sweep}
\begin{tabular}{lc}
\toprule
\textit{Run} & val\_auc \\
\midrule
\texttt{mamba\_small\_locked} (\textit{pointer histórico}) & 0.750 \\
\midrule
\texttt{mamba\_small\_seed42}    & 0.7398 \\
\texttt{mamba\_small\_seed123}   & 0.7237 \\
\texttt{mamba\_small\_seed456}   & 0.6682 \\
\texttt{mamba\_small\_seed789}   & 0.7632 \\
\texttt{mamba\_small\_seed2024}  & 0.7601 \\
\midrule
\textbf{Media $\pm$ desv.\ estándar} & \textbf{$0.7310 \pm 0.0386$} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Curvas de aprendizaje y diagnóstico de \textit{overfitting}/\textit{underfitting}}
\label{subsec:learning-curves}

Inspeccionar las curvas de entrenamiento época por época permite
diagnosticar el régimen en el que está operando el modelo: si la
pérdida en \textit{train} y la pérdida en \textit{val} bajan
conjuntamente, el modelo aprende sin sobreajustar; si la
\textit{train loss} se desploma pero la \textit{val loss} se estanca
o crece, hay \textit{overfitting}; si ninguna de las dos baja, hay
\textit{underfitting} (capacidad insuficiente o problema de
optimización). La Figura~\ref{fig:learning-curves} muestra los cuatro
paneles diagnósticos para la mejor corrida del \textit{multi-seed
sweep} (\textit{seed} 789), que es representativa del comportamiento
de Mamba bajo la configuración \textit{locked}.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.95\textwidth]{figures/learning_curves_mamba_seed789.png}
\caption{Curvas de aprendizaje del Mamba \textit{single-view} para la
\textit{seed} 789 (mejor del \textit{sweep}). \textbf{Arriba
izquierda:} pérdidas de \textit{train} y \textit{validation} (BCE)
en función de la época. \textbf{Arriba derecha:} AUC-ROC y AUC-PR
sobre \textit{validation}; línea vertical en la mejor época,
seleccionada para el \textit{checkpoint} \texttt{best.pt}.
\textbf{Abajo izquierda:} \textit{learning rate} con
\textit{cosine annealing}. \textbf{Abajo derecha:} brecha
$\text{val\_loss} - \text{train\_loss}$ como indicador de
\textit{overfitting}.}
\label{fig:learning-curves}
\end{figure}

\paragraph{Lectura del entrenamiento.}
La curva de \textit{train loss} muestra una reducción sostenida
durante todo el entrenamiento: el modelo está aprendiendo a ajustarse
a los datos. La curva de \textit{val loss} también baja en las
primeras épocas pero a una tasa menor, y se estabiliza antes que la
de \textit{train}. La AUC-ROC sobre \textit{validation} crece de
forma consistente con la \textit{val loss} y alcanza su máximo en la
época 25, donde se guarda el \textit{checkpoint} \texttt{best.pt}
que se evaluará después en el \textit{test} sellado
(Sec.\ref{sec:results}). El \textit{schedule} de \textit{cosine
annealing} baja la tasa de aprendizaje suavemente desde
$1.5\!\times\!10^{-3}$ hasta valores cercanos al mínimo configurado
en la última fracción del entrenamiento, lo que permite una fase
final de ajuste fino sin grandes saltos.

\paragraph{Diagnóstico de \textit{overfitting}/\textit{underfitting}.}
El panel inferior derecho muestra la diferencia entre
\textit{val loss} y \textit{train loss} en cada época. Esta brecha
es un indicador estándar de sobreajuste: si crece sin freno, el
modelo se está memorizando \textit{train} sin generalizar. En esta
corrida la brecha es positiva (lo esperable, pues \textit{train} es
más fácil) y crece moderadamente con las épocas, pero la AUC-ROC
sobre \textit{validation} no colapsa: sigue oscilando alrededor de
su máximo sin caer abruptamente. El \textit{early stopping} con
paciencia 10 detiene el entrenamiento poco después del máximo de
\textit{val\_auc} para evitar entrar en una región de sobreajuste
explícito. En suma, el régimen observado no es \textit{overfitting}
severo ni \textit{underfitting}: el modelo aprende, generaliza
parcialmente, y la separación entre \textit{train} y
\textit{validation} es consistente con el tamaño reducido del
\textit{dataset} (1\,103 ejemplos en \textit{train}), donde es
esperable cierto grado de memorización. La estrategia de
\textit{ensemble} multi-semilla (Sec.\ref{sec:results}) suaviza
adicionalmente este efecto al promediar predicciones de modelos
entrenados con inicializaciones distintas.

\subsection{Justificación de no usar \textit{grid search} ni Optuna}
\label{subsec:why-no-grid}

El estándar de oro para la selección de hiperparámetros es una
\textbf{búsqueda sistemática} (\textit{grid search}, búsqueda
aleatoria, o métodos bayesianos como Optuna) combinada con validación
cruzada. La rúbrica del curso reconoce este estándar como
\textit{``Excelente''}.

Aplicarlo aquí no es viable. Una sola época de Mamba toma del orden de
5 minutos en la RTX 3050 disponible; un entrenamiento completo de 50
épocas, cerca de una hora. Una búsqueda sistemática modesta con 20
configuraciones y validación cruzada $K=5$ implicaría
$20 \times 5 \approx 100$ horas de GPU continuas para un único modelo.
El presupuesto de cómputo del curso no permite esa magnitud.

\paragraph{Trade-off explícito.}
La estrategia adoptada (ajuste manual acotado + sanity overfit +
\textit{multi-seed sweep} sobre la configuración congelada) cubre
los tres requisitos que la rúbrica considera importantes:
\textit{búsqueda},
\textit{justificación de costo} y
\textit{reproducibilidad}.
La búsqueda existe (variables exploradas listadas arriba), su costo
está documentado, y todos los \textit{runs} se pueden reproducir
ejecutando \texttt{python scripts/train.py --config configs/mamba\_small.yaml
--seed <s>}. Lo que se sacrifica es la cobertura del espacio de
hiperparámetros, que se asume menos crítica que la calidad de cada
\textit{run} individual sobre este \textit{dataset} pequeño.

\subsection{Reproducibilidad por \textit{run}}
\label{subsec:run-repro}

Cada vez que se entrena un modelo, el \textit{script} crea un
directorio \texttt{experiments/<timestamp>\_<nombre>/} con los
artefactos necesarios para reproducirlo o auditarlo. Esto implementa
de forma operativa la \textit{``ejecución robusta + configs''} que la
rúbrica pide en el eje de \textit{Ingeniería} (Sec.\ref{sec:repro}).

\begin{itemize}
  \item \texttt{config.yaml}: copia exacta del archivo de
    configuración usado. Modificar el YAML después del \textit{run} no
    afecta este registro.
  \item \texttt{env\_info.txt}: versión de Python, \texttt{torch},
    CUDA, \texttt{mamba-ssm} y otras dependencias claves, además del
    sistema operativo.
  \item \texttt{git\_info.txt}: \textit{commit hash} y estado del
    \textit{working tree} en el momento del \textit{run}. Si el
    código se modifica después, este archivo permite reconstruir la
    versión exacta que se ejecutó.
  \item \texttt{train.log}: log de entrenamiento con la métrica de
    cada época.
  \item \texttt{metrics.csv}: métricas tabulares (\textit{train
    loss}, \textit{val loss}, \textit{val\_auc}, \textit{lr}, etc.)
    consumibles por herramientas de análisis.
  \item \texttt{checkpoints/\{best,last\}.pt}: pesos del modelo en
    la mejor época según \textit{val} y al final del entrenamiento.
  \item \texttt{eval\_test/} (poblado al evaluar): métricas y
    predicciones sobre el \textit{test} sellado.
\end{itemize}

Esta convención significa que un tercero con acceso al repositorio
puede, dado solo el \textit{run\_dir}, saber exactamente qué se
entrenó, con qué versión del código, sobre qué datos, y con qué
resultado.

% ============================================================
\section{Métricas y resultados}
\label{sec:results}

\subsection{Métricas utilizadas}
\label{subsec:metrics}

El \textit{dataset} tiene clases desbalanceadas (38\,\% CP frente a
62\,\% FP). En ese régimen la \textit{accuracy} es engañosa: un
clasificador trivial que prediga siempre la clase mayoritaria alcanza
cerca de 62\,\% de \textit{accuracy} sin haber aprendido nada. Por eso
se reportan métricas que sí distinguen el aporte real del modelo a la
clase positiva:

\begin{itemize}
  \item \textbf{AUC-ROC}: área bajo la curva ROC. Invariante al
    \textit{threshold} de decisión; mide la separación global entre
    distribuciones de probabilidad de las dos clases. Valor $0.5$
    corresponde al azar; $1.0$ a separación perfecta.
  \item \textbf{AUC-PR}: área bajo la curva
    \textit{precision-recall}. Más sensible que AUC-ROC cuando la
    clase positiva es minoritaria.
  \item \textbf{F1, Recall, Precision} (con $\text{threshold} = 0.5$).
    Recall es especialmente relevante en \textit{vetting} de
    exoplanetas: no detectar un planeta real (FN) es más costoso que
    investigar un falso positivo extra.
  \item \textbf{Brier score}:
    $\mathrm{Brier} = \frac{1}{N}\sum_{i=1}^{N}(y_i - \hat{p}_i)^2$,
    mide calibración de las probabilidades emitidas. Más bajo es
    mejor.
\end{itemize}

La Tabla~\ref{tab:metric-intuition} resume las mismas métricas en
lenguaje intuitivo, sin asumir familiaridad previa con curvas ROC ni
\textit{thresholds} de clasificación.

\begin{table}[H]
\centering
\caption{Interpretación intuitiva de las métricas usadas en este
reporte.}
\label{tab:metric-intuition}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{p{0.18\textwidth} p{0.72\textwidth}}
\toprule
\textbf{Métrica} & \textbf{Interpretación intuitiva} \\
\midrule
AUC-ROC   & Qué tan bien separa el modelo planetas reales de falsos
            positivos, sin fijar un umbral específico. \\
AUC-PR    & Qué tan útil es el modelo cuando importa mucho encontrar
            la clase positiva, en este caso los planetas confirmados. \\
Recall    & De todos los planetas reales, cuántos logra recuperar el
            modelo. \\
Precision & De todo lo que el modelo marcó como planeta, cuánto
            realmente era planeta. \\
F1        & Balance entre \textit{precision} y \textit{recall}. \\
Brier     & Qué tan bien calibradas están las probabilidades: si el
            modelo dice 80\,\%, debería acertar cerca de 80\,\% de las
            veces en casos similares. \\
\bottomrule
\end{tabular}
\end{table}

Durante el entrenamiento se optimiza la entropía cruzada binaria con
peso para corregir el desbalance:
\begin{equation}
\mathcal{L}_{\text{BCE}} = -\frac{1}{N}\sum_{i=1}^{N}
  \bigl[ w_+ \, y_i \log \hat{p}_i + (1-y_i) \log(1-\hat{p}_i) \bigr],
\quad w_+ = \frac{N_{\text{FP}}}{N_{\text{CP}}},
\end{equation}
con $w_+ \approx 1.61$ en este \textit{dataset}, lo que penaliza más
los errores sobre la clase CP minoritaria.

\subsection{Tabla principal}
\label{subsec:main-results}

La Tabla~\ref{tab:main-results} reporta el desempeño de los cuatro
modelos sobre el \textit{test} sellado ($N = 237$, 91 CP y 146 FP).
Cada fila Mamba con etiqueta de semilla corresponde a un
entrenamiento independiente sobre la arquitectura congelada; el
\textit{ensemble} promedia las probabilidades $\hat{p}_i$ de las
cinco semillas. La columna de \textit{ensemble} es la métrica final
reportada para el modelo entregable.

\begin{table}[H]
\centering
\caption{Métricas en \textit{test} sellado. Para LogReg no se reporta
\textit{Brier} porque sus probabilidades completas no se conservaron
en el artefacto de evaluación original; Random emite una constante,
por lo que el \textit{Brier} reportado en su fila corresponde al
error cuadrático del prior $\hat{p} \approx 0.383$. El
\textit{ensemble} es promedio de probabilidades sobre las cinco
semillas Mamba.}
\label{tab:main-results}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{l c c c c c c}
\toprule
Modelo & AUC-ROC & AUC-PR & F1 & Recall & Precision & Brier \\
\midrule
Random estratificado          & 0.500 & 0.384 & 0.000 & 0.000 & 0.000 & 0.236 \\
LogReg (catálogo)             & 0.605 & 0.464 & 0.486 & 0.571 & 0.423 & n/a \\
CNN \textit{single-branch}    & 0.604 & 0.551 & 0.539 & 0.758 & 0.418 & 0.271 \\
Mamba \textit{single} (locked)& 0.763 & 0.650 & 0.633 & 0.835 & 0.510 & 0.210 \\
\midrule
\multicolumn{7}{l}{\textit{Multi-seed sweep sobre la arquitectura locked:}} \\
\quad seed 42                 & 0.757 & 0.645 & 0.652 & 0.835 & 0.535 & 0.210 \\
\quad seed 123                & 0.758 & 0.680 & 0.622 & 0.824 & 0.500 & 0.250 \\
\quad seed 456                & 0.628 & 0.530 & 0.513 & 0.560 & 0.472 & 0.232 \\
\quad seed 789                & 0.810 & 0.722 & 0.661 & 0.802 & 0.562 & 0.190 \\
\quad seed 2024               & 0.796 & 0.714 & 0.649 & 0.670 & 0.629 & 0.180 \\
\midrule
\textbf{Mamba ensemble 5s}    & \textbf{0.806} & \textbf{0.711} & \textbf{0.679} & 0.824 & 0.577 & \textbf{0.192} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Lectura.}
El salto cualitativo más grande se produce entre el CNN
\textit{single-branch} ($0.604$ AUC) y Mamba \textit{single-view}
\textit{locked} ($0.763$): un incremento de $+15.9$ puntos
porcentuales atribuible al cambio de arquitectura, manteniendo
constante el resto del \textit{pipeline} (mismas curvas, mismo
\textit{split}, misma normalización, mismas \textit{augmentations}).
La regresión logística sobre metadata del catálogo alcanza $0.605$,
prácticamente empatada con el CNN, lo que sugiere que la convolución
poco profunda no extrae señal sustancialmente mayor que la disponible
en los \textit{features} tabulares. Mamba, en cambio, sí explota la
estructura temporal de la curva.

El \textit{multi-seed sweep} confirma la robustez del resultado:
cuatro de cinco semillas caen en el rango $[0.757, 0.810]$, con una
\textit{seed} atípica ($456$, $0.628$) que sugiere sensibilidad de la
inicialización a este \textit{dataset} pequeño. El \textit{ensemble}
estabiliza esta variabilidad: al promediar las probabilidades, las
predicciones individuales menos confiables se suavizan y la
\textit{seed} atípica deja de penalizar el resultado. El AUC-ROC del
\textit{ensemble} ($0.806$) supera la media simple de las cinco
semillas ($0.731$) y se acerca al mejor caso individual ($0.810$),
patrón típico de \textit{ensembling} eficaz.

\subsection{Curva ROC comparativa}
\label{subsec:roc}

La Figura~\ref{fig:roc-tier1} muestra las curvas ROC de los modelos
sobre el \textit{test} sellado. La separación visual entre Mamba
\textit{ensemble} y los baselines refuerza la lectura de
Tabla~\ref{tab:main-results}: la mejora no se concentra en una región
particular del espacio de \textit{thresholds} sino que es uniforme a
lo largo de la curva.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.78\textwidth]{figures/roc_tier1.png}
\caption{Curvas ROC sobre el \textit{test} sellado para los cuatro
modelos. La curva Mamba \textit{ensemble} domina al resto en todo el
rango de \textit{thresholds}, evidenciando que el aporte del SSM
sobre la convolución es sistemático y no dependiente del punto de
operación elegido.}
\label{fig:roc-tier1}
\end{figure}

\subsection{Matriz de confusión y calibración del \textit{ensemble}}
\label{subsec:confusion-calibration}

La Figura~\ref{fig:cm-cal} desglosa el comportamiento del
\textit{ensemble} a \textit{threshold} = 0.5. La matriz de confusión
muestra que el modelo recupera cerca del 82\,\% de los CP del
\textit{test} (recall), al costo de cerca del 42\,\% de FP que pasan el
umbral (precisión $0.58$). En \textit{vetting} de exoplanetas este
\textit{trade-off} suele ser preferible al inverso: vale la pena
investigar manualmente un FP extra a costa de no perder un CP real.
La curva de calibración compara la probabilidad predicha contra la
proporción real de positivos en cada \textit{bin}. La cercanía a la
diagonal indica que las probabilidades emitidas son razonablemente
confiables como medida de confianza, no solo como criterio binario.

\begin{figure}[H]
\centering
\begin{subfigure}{0.48\textwidth}
  \safeincludegraphics[width=\textwidth]{results/mamba_ensemble/confusion_matrix.png}
  \caption{Matriz de confusión $@\ \text{threshold} = 0.5$.}
\end{subfigure}
\hfill
\begin{subfigure}{0.48\textwidth}
  \safeincludegraphics[width=\textwidth]{results/mamba_ensemble/calibration.png}
  \caption{Curva de calibración (\textit{reliability diagram}).}
\end{subfigure}
\caption{Mamba \textit{ensemble} de cinco semillas: comportamiento a
\textit{threshold} de operación y calibración de probabilidades.}
\label{fig:cm-cal}
\end{figure}

% ============================================================
\section{Análisis de errores}
\label{sec:errors}

Una métrica agregada como AUC-ROC = $0.806$ resume el desempeño en
un solo número, pero no dice \textit{dónde} falla el modelo. El
análisis de errores busca responder esa pregunta: ¿qué tipo de casos
clasifica mal el Mamba \textit{ensemble} y por qué? Identificar el
patrón permite, eventualmente, proponer mejoras dirigidas.

\subsection{Distribución de probabilidades por clase verdadera}
\label{subsec:prob-hist}

La Figura~\ref{fig:prob-hist} muestra el histograma de la
probabilidad predicha $\hat{p}$ separado por la clase verdadera. Un
clasificador ideal mostraría dos modas claras: los FP concentrados
cerca de $0$ y los CP concentrados cerca de $1$. El histograma real
exhibe una bimodalidad parcial pero con masa significativa en la
zona intermedia $[0.3, 0.7]$ para ambas clases, especialmente en los
CP. Esa masa intermedia es la fuente de la mayor parte de los
errores: el modelo identifica correctamente los casos extremos pero
duda en los casos ambiguos, donde la señal de tránsito está cerca del
umbral de detección.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.72\textwidth]{results/error_analysis/mamba_ensemble/prob_histogram.png}
\caption{Histograma de la probabilidad predicha $\hat{p}$ por el
Mamba \textit{ensemble}, separado por clase verdadera. La bimodalidad
parcial indica separación correcta en los casos confidentes; la masa
en $[0.3, 0.7]$ explica la mayoría de los errores.}
\label{fig:prob-hist}
\end{figure}

\subsection{Tasa de error por \textit{feature} física}
\label{subsec:error-features}

Para identificar si el modelo falla más en ciertas regiones del
espacio físico, se divide el conjunto de \textit{test} en \textit{bins}
sobre tres parámetros del catálogo TOI: el período orbital
(\texttt{pl\_orbper}), la profundidad del tránsito
(\texttt{pl\_trandep}) y la magnitud TESS de la estrella
(\texttt{st\_tmag}); luego se calcula la tasa de error dentro de cada
\textit{bin}. La Figura~\ref{fig:error-features} muestra los tres
paneles.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.95\textwidth]{results/error_analysis/mamba_ensemble/error_rate_by_feature.png}
\caption{Tasa de error del Mamba \textit{ensemble} en función de
\texttt{pl\_orbper}, \texttt{pl\_trandep} y \texttt{st\_tmag}.
Permite identificar regiones del espacio físico donde el modelo es
sistemáticamente menos confiable.}
\label{fig:error-features}
\end{figure}

\paragraph{Lectura.}
La tasa de error tiende a ser mayor para tránsitos
\textbf{poco profundos} (\texttt{pl\_trandep} bajo) y para estrellas
con magnitud TESS alta (estrellas más débiles, con mayor ruido
fotométrico). Esto es consistente con la intuición física: la señal
de tránsito sobre estos casos está cerca del piso de ruido y resulta
genuinamente más difícil de distinguir de FP. Por el lado del
período orbital la relación es menos clara, sugiriendo que el modelo
no privilegia particularmente períodos cortos o largos.

\subsection{Casos más confidentes mal clasificados}
\label{subsec:top-errors}

La Figura~\ref{fig:top-errors} presenta las diez curvas de luz que el
modelo clasifica con mayor confianza errada en cada dirección: los
top-10 falsos negativos (CP que el modelo declara FP con
$\hat{p} \ll 0.5$) y los top-10 falsos positivos (FP que el modelo
declara CP con $\hat{p} \gg 0.5$). Estos son los casos donde el
modelo está más equivocado.

\begin{figure}[H]
\centering
\begin{subfigure}{0.48\textwidth}
  \safeincludegraphics[width=\textwidth]{results/error_analysis/mamba_ensemble/top_fn_curves.png}
  \caption{Top-10 falsos negativos (CP clasificados como FP).}
\end{subfigure}
\hfill
\begin{subfigure}{0.48\textwidth}
  \safeincludegraphics[width=\textwidth]{results/error_analysis/mamba_ensemble/top_fp_curves.png}
  \caption{Top-10 falsos positivos (FP clasificados como CP).}
\end{subfigure}
\caption{Curvas de los casos más confidentes mal clasificados por el
Mamba \textit{ensemble}. Los FN típicos son tránsitos poco profundos
o curvas ruidosas; los FP típicos son curvas con \textit{dips}
periódicos que el modelo confunde con tránsitos planetarios (suelen
ser eclipses estelares).}
\label{fig:top-errors}
\end{figure}

Los listados completos en CSV (\texttt{top\_fn.csv}, \texttt{top\_fp.csv})
están disponibles en \texttt{paper/results/error\_analysis/mamba\_ensemble/}.
Estos casos serían los primeros candidatos para inspección manual
por un astrónomo si este modelo se desplegase como herramienta de
\textit{vetting} asistida.

% ============================================================
\section{Interpretabilidad: XAI}
\label{sec:xai}

Las métricas y el análisis de errores describen \textit{cuánto} se
equivoca el modelo. La interpretabilidad (\textit{XAI},
\textit{eXplainable AI}) intenta responder una pregunta distinta:
\textit{¿en qué se fija el modelo para tomar su decisión?} Esta
sección aplica tres técnicas complementarias sobre el Mamba
entrenado para inspeccionar, sobre casos concretos, qué partes de la
curva de luz pesan más en la predicción.

\paragraph{Sobre la elección de métodos.}
Mamba no implementa \textit{attention} en el sentido del Transformer,
así que no se pueden visualizar mapas de atención como en BERT o
ViT. Los métodos elegidos aquí son agnósticos a la arquitectura: solo
requieren poder propagar gradientes a través del modelo
(\textit{saliency} e \textit{integrated gradients}) o ejecutar
\textit{forward passes} con perturbaciones controladas
(\textit{occlusion}).

\subsection{Métodos aplicados}
\label{subsec:xai-methods}

\paragraph{Gradient saliency.}
La técnica más directa. Mide cuánto cambia la probabilidad predicha
ante un cambio infinitesimal en cada punto de la curva:
\begin{equation}
s_i \;=\; \left|\dfrac{\partial \hat{p}}{\partial x_i}\right|.
\label{eq:saliency}
\end{equation}
Valores altos de $s_i$ indican puntos donde el modelo es sensible: si
ese punto cambiara, su decisión cambiaría más. Limitación: \textit{el
gradiente puede ser ruidoso} y a veces resalta artefactos locales más
que estructura semántica.

\paragraph{Integrated Gradients (IG).}
Mitiga el ruido del \textit{saliency} simple integrando el gradiente
a lo largo de un camino desde una curva de referencia $x'$ (en este
trabajo, la curva nula $x' = \mathbf{0}$) hasta la curva de interés
$x$:
\begin{equation}
\mathrm{IG}_i(x) \;=\; (x_i - x'_i)\int_0^1
\frac{\partial \hat{p}\bigl(x' + \alpha (x - x')\bigr)}{\partial x_i}\,d\alpha.
\label{eq:ig}
\end{equation}
Esta integral se aproxima numéricamente con 50 puntos por suma de
Riemann. IG cumple dos propiedades teóricas deseables:
\textit{sensibilidad} (si dos curvas difieren en un solo punto y
producen predicciones distintas, ese punto recibe atribución no
nula) y \textit{completitud} (la suma de atribuciones iguala
$\hat{p}(x) - \hat{p}(x')$).

\paragraph{Occlusion sensitivity.}
Evalúa empíricamente la importancia de cada región ocluyéndola y
midiendo cuánto cae la predicción. Para una ventana de ancho $w$
centrada en $i$:
\begin{equation}
o_i \;=\; \hat{p}(x) - \hat{p}\bigl(x_{[i-w:i+w] \leftarrow 1.0}\bigr),
\label{eq:occlusion}
\end{equation}
es decir, reemplazar la ventana por valor neutro $1.0$ (la mediana
post-normalización) y medir el cambio. Las regiones cuyo ocluido
\textbf{baja} la predicción son aquellas que el modelo usa como
evidencia positiva del tránsito. A diferencia de los gradientes, este
método no usa información local del modelo: prueba directamente qué
pasa al modificar el \textit{input}.

\subsection{Política de selección de casos}
\label{subsec:xai-selection}

Aplicar los tres métodos sobre las 237 curvas del \textit{test} no es
útil: produciría demasiada información. En su lugar se inspecciona un
conjunto reducido de \textbf{ocho casos representativos}, dos por
cada cuadrante de la matriz de confusión: dos TP, dos TN, dos FN y
dos FP. Dentro de cada cuadrante se eligen los dos casos con mayor
confianza, definida como
$\bigl|\hat{p} - 0.5\bigr|$. Esto selecciona, en TP y TN, los
ejemplos donde el modelo acertó con más seguridad (esperamos
saliency limpia), y en FN y FP, los ejemplos donde el modelo se
equivocó con más seguridad (esperamos identificar qué llevó al error).

El modelo analizado es Mamba \textit{seed} 789, el mejor del
\textit{multi-seed sweep}. La selección y generación se hace con
\texttt{scripts/run\_xai.py}; las figuras viven en
\texttt{paper/figures/xai/mamba\_seed789/}.

\subsection{Hallazgos}
\label{subsec:xai-findings}

La Figura~\ref{fig:xai-summary} consolida los ocho casos en una
visualización conjunta: cada fila es un caso, cada columna un método
de atribución. Permite ver de un vistazo qué partes de cada curva
fueron determinantes según los tres puntos de vista.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.95\textwidth]{figures/xai/mamba_seed789/_summary.png}
\caption{Resumen XAI sobre los ocho casos seleccionados (dos por
cuadrante TP/TN/FN/FP) para Mamba \textit{seed} 789. Cada caso se
analiza con los tres métodos: \textit{saliency} de gradiente,
\textit{integrated gradients} y \textit{occlusion sensitivity}.}
\label{fig:xai-summary}
\end{figure}

La Figura~\ref{fig:xai-tp-zoom} amplía uno de los TP confidentes
(TIC 218795833). El mapa de \textit{saliency} muestra picos
concentrados sobre la región del \textit{dip} de tránsito y atenuados
fuera de ella, evidencia directa de que el modelo basa su decisión
en la morfología correcta de la señal y no en artefactos del
\textit{baseline}.

\begin{figure}[H]
\centering
\safeincludegraphics[width=0.85\textwidth]{figures/xai/mamba_seed789/TP_1_tic218795833_saliency.png}
\caption{\textit{Saliency} sobre un TP confidente (TIC 218795833,
$\hat{p} \approx 0.95$). El gradiente se concentra en la región del
\textit{dip} de tránsito; el resto de la curva contribuye marginalmente
a la decisión.}
\label{fig:xai-tp-zoom}
\end{figure}

\begin{tcolorbox}[colback=blue!5,colframe=blue!50!black,
  title=Hallazgo XAI principal]
En los TP confidentes los tres métodos coinciden: la atribución se
concentra en las regiones de \textit{dip} de la curva. En los FN
confidentes, en cambio, la \textit{saliency} aparece dispersa a lo
largo de toda la serie temporal, sin un pico claro. Esto sugiere que
los casos donde el modelo \textit{cree con seguridad} que no hay
planeta corresponden a curvas en las que efectivamente no encontró
una estructura coherente en la que basar la decisión; el modo de
falla no es un error de razonamiento sino de detección. Esto se
alinea con el análisis de errores de Sec.\ref{subsec:error-features}:
los FN se concentran en curvas con tránsitos poco profundos donde la
señal está cerca del piso de ruido.
\end{tcolorbox}

% ============================================================
\section{Ingeniería y reproducibilidad}
\label{sec:repro}
% RUBRIC EXCELENTE: repo limpio + README completo + seeds + configs + ejecución robusta.

\subsection{Estructura del repositorio}
\begin{lstlisting}[caption={Árbol del repositorio (top-level).}]
mamba-exoplanet/
+-- configs/             # YAMLs por experimento (versionados)
+-- data/
|   +-- splits/          # CSVs train/val/test por TIC (versionados)
|   +-- raw/processed/   # gitignored
+-- src/exoplanet/       # paquete instalable (src layout)
|   +-- data/ models/ training/ evaluation/ utils/
+-- scripts/             # CLIs reproducibles (train, evaluate, preprocess)
|   +-- wsl2/            # helpers shell para entorno WSL2
+-- experiments/         # outputs por run (gitignored)
+-- paper/               # reporte + figuras + resultados
+-- notebooks/           # exploración numerada
+-- tests/               # 8 archivos, 49 tests
+-- docs/                # documentación interna
+-- pyproject.toml       # PEP 621 + ruff + pytest
+-- README.md            # 10 pasos para reproducir Etapa 2
\end{lstlisting}

\subsection{Configs YAML versionados}
\label{subsec:configs}

Cada experimento del proyecto es \textit{una} corrida de
\texttt{scripts/train.py --config <archivo.yaml>}. El YAML contiene
todos los hiperparámetros que afectan el resultado: datos
(\texttt{train\_csv}, \texttt{val\_csv}, \texttt{batch\_size}),
modelo (\texttt{type} y \texttt{params}), entrenamiento (optimizador,
\textit{scheduler}, épocas, FP16, \textit{gradient checkpointing}),
\textit{early stopping}, \textit{augmentation} y \textit{logging}.
Las configuraciones canónicas viven en \texttt{configs/} versionadas
en el repositorio:
\texttt{random\_baseline.yaml},
\texttt{cnn\_baseline.yaml} y
\texttt{mamba\_small.yaml}.

\subsection{Semillas y determinismo}
\label{subsec:seeds}

La función \texttt{exoplanet.utils.set\_seed(seed)} configura los
generadores aleatorios de PyTorch (CPU y CUDA), NumPy y la librería
estándar \texttt{random}, además de poner \texttt{torch.backends.\linebreak%
cudnn.deterministic} en modo determinístico. Tres pruebas en
\texttt{tests/test\_seeds.py} verifican que con la misma semilla se
producen exactamente las mismas secuencias y que con semillas
distintas las secuencias difieren (Sec.\ref{sec:tests}).

\subsection{Artefactos por \textit{run}}
\label{subsec:run-artifacts}

Cada \textit{run} crea un directorio
\texttt{experiments/<timestamp>\_<nombre>/} con los siguientes
artefactos, los mismos siete que se documentan en
Sec.\ref{subsec:run-repro}. La duplicación es deliberada: la
Sec.\ref{subsec:run-repro} los explica desde la perspectiva del
\textit{tuning}; aquí los listamos como infraestructura común a
todos los modelos del estudio.
\begin{itemize}
  \item \texttt{config.yaml}: copia exacta del YAML usado.
  \item \texttt{env\_info.txt}: versión de Python, \texttt{torch},
    CUDA, \texttt{mamba-ssm}, y otras dependencias.
  \item \texttt{git\_info.txt}: \textit{commit hash} + estado del
    \textit{working tree} al ejecutar.
  \item \texttt{train.log}: log epoch-by-epoch.
  \item \texttt{metrics.csv}: métricas tabulares.
  \item \texttt{checkpoints/\{best,last\}.pt}: pesos del modelo.
  \item \texttt{eval\_test/}: métricas de \textit{test} sellado
    (poblado una sola vez).
\end{itemize}

\subsection{Entornos: Windows nativo vs WSL2}
\begin{table}[H]
\centering
\caption{Entornos canónicos.}
\begin{tabular}{lll}
\toprule
Componente & Pipeline general (Windows) & Modelo Mamba (WSL2 Ubuntu 24.04) \\
\midrule
Python      & 3.11.9              & 3.11                          \\
torch       & 2.11.0+cu128        & 2.5.1+cu121                   \\
mamba-ssm   & n/a                 & 2.2.6                          \\
CUDA driver & 581.83              & 12.1                           \\
GPU         & RTX 3050 4GB        & RTX 3050 4GB                   \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Suite de tests}
\label{sec:tests}
% Reusar docs/tests_overview.md.
% Total: 8 archivos, 49 tests.

\subsection{Estrategia general}
% (1) Smoke / contratos: test_smoke.py, test_seeds.py.
% (2) Componentes aislados: test_augment.py, test_collate.py,
%     test_metrics.py, test_cnn_baseline.py.
% (3) End-to-end con skip-si-no-hay-datos: test_dataset.py, test_training_smoke.py.

\begin{table}[H]
\centering
\caption{Cobertura de la suite de pruebas.}
\label{tab:tests}
\begin{tabular}{llc}
\toprule
Archivo & Cubre & Tests \\
\midrule
\texttt{test\_smoke.py}           & Imports del paquete y subpaquetes        & 2 \\
\texttt{test\_seeds.py}           & set\_seed() reproducible torch + numpy   & 3 \\
\texttt{test\_augment.py}         & 4 augmentations + Compose + registry     & 21 \\
\texttt{test\_collate.py}         & Batching con campos opcionales           & 4 \\
\texttt{test\_metrics.py}         & AUC/F1/Recall/Precision casos canónicos  & 4 \\
\texttt{test\_cnn\_baseline.py}   & Forward, backward, params, kwargs        & 4 \\
\texttt{test\_dataset.py}         & \texttt{LightCurveDataset}: schema y \textit{loading} & 5 \\
\texttt{test\_training\_smoke.py} & End-to-end con \texttt{configs/smoke.yaml}            & 3 \\
\addlinespace
Otros (\textit{loaders}, \textit{utils})  &                                  & 3 \\
\midrule
\textbf{Total}                    &                                          & \textbf{49} \\
\bottomrule
\end{tabular}
\end{table}

% Detallar bloque por bloque (ver docs/tests_overview.md).

% ============================================================
\section{Discusión}
\label{sec:discussion}

\paragraph{Mamba supera a la convolución pura.}
El resultado central del estudio es el salto en AUC-ROC entre el CNN
\textit{single-branch} ($0.604$) y Mamba \textit{single-view}
\textit{locked} ($0.763$): $+15.9$ puntos porcentuales bajo control
estricto del resto del \textit{pipeline}. El \textit{ensemble} de
cinco semillas Mamba sube esa cifra a $0.806$. Este resultado
respalda la hipótesis inicial: para secuencias largas como las curvas
de luz de TESS ($L = 18\,000$), un modelo selectivo de espacio de
estados aprovecha mejor la estructura temporal que una CNN poco
profunda con \textit{kernels} locales.

\paragraph{La regresión logística sobre catálogo.}
LogReg sobre \textit{features} tabulares del catálogo TOI obtiene
AUC-ROC $0.605$, prácticamente empatada con el CNN
\textit{single-branch} ($0.604$). Esto significa que, para esta
arquitectura CNN, el aprendizaje sobre la curva no extrae señal
significativamente mayor que la presente en cuatro \textit{features}
del catálogo. Mamba, en cambio, sí mejora claramente sobre LogReg
($+20.1$ pp), evidencia de que la información temporal de la curva
existe pero requiere una arquitectura adecuada para extraerla.

\paragraph{Variabilidad y \textit{ensembling}.}
El \textit{multi-seed sweep} reveló una semilla atípica
(\textit{seed} 456 con AUC $0.628$), del orden del CNN. Cuatro de
cinco semillas caen en $[0.757, 0.810]$, lo que indica que la
arquitectura Mamba es robusta pero sensible a inicialización en un
\textit{dataset} de este tamaño. El \textit{ensemble} suaviza ese
ruido y se acerca al mejor caso individual ($0.806$ vs $0.810$).
Este patrón valida \textit{a posteriori} la decisión metodológica de
reportar la métrica del \textit{ensemble} como número final en lugar
de la mejor semilla individual.

\paragraph{Umbrales aspiracionales del proyecto.}
La propuesta de Etapa 1 fijó como umbral de \textit{``Excelente''} un
AUC-ROC $\geq 0.93$. El resultado obtenido (\textit{ensemble}
$0.806$) queda por debajo de ese umbral, en línea con lo declarado en
la propia propuesta: los umbrales eran \textit{aspiracionales y no
contractuales}. La frase usada como protección defensiva en la
propuesta original aplica directamente aquí: el objetivo no era
garantizar un número absoluto sino evaluar empíricamente si Mamba
ofrece ventajas medibles sobre la convolución bajo restricciones
realistas de datos y hardware. El estudio muestra que sí: $+15.9$ pp
de AUC sobre el CNN con la misma entrada y un costo de cómputo del
mismo orden.

\paragraph{Modos de falla del modelo.}
El análisis de errores (Sec.\ref{sec:errors}) y el análisis XAI
(Sec.\ref{sec:xai}) llegan al mismo diagnóstico desde dos vías
distintas, y vale la pena desglosarlo. Los errores del Mamba
\textit{ensemble} no están distribuidos al azar entre los TICs del
\textit{test}: se concentran sistemáticamente en dos condiciones
físicas concretas.

\textbf{Tránsitos poco profundos.}
La señal que el modelo busca es una baja momentánea del brillo de la
estrella debida a la sombra del planeta. Esa baja se mide en partes
por millón (ppm): cuando un planeta es pequeño respecto a su
estrella, tapa una fracción minúscula de la luz, y el ``dip''
correspondiente apenas se distingue de la variabilidad normal de la
fotometría. En términos del catálogo TOI, los falsos negativos del
modelo coinciden con \textit{bins} bajos de \texttt{pl\_trandep}, lo
que se ve directamente en la Figura~\ref{fig:error-features}: la
tasa de error sube cuando la profundidad del tránsito disminuye.

\textbf{Estrellas más débiles.}
TESS mide brillo contando fotones. Una estrella más débil (mayor
magnitud TESS) emite menos fotones por minuto, y por estadística de
Poisson el ruido relativo de la medición crece con la raíz cuadrada
del número de fotones recibidos. Esto significa que las curvas de
luz de estrellas débiles son intrínsecamente más ruidosas, con
fluctuaciones aleatorias que pueden enmascarar o imitar un tránsito.
La tasa de error del modelo también sube con \texttt{st\_tmag}, como
se ve en el mismo panel de Figura~\ref{fig:error-features}.

\textbf{Por qué el modelo se equivoca aquí.}
Cuando ambas condiciones coinciden (tránsito leve sobre estrella
débil), la relación señal-ruido (SNR, \textit{signal-to-noise
ratio}) es tan baja que el \textit{dip} del tránsito es comparable a
las fluctuaciones del ruido fotométrico. El análisis XAI confirma
este diagnóstico mirando el problema desde el otro lado: en los
falsos negativos confidentes, la atribución de gradientes no muestra
un pico claro en ninguna región específica de la curva, está
dispersa a lo largo de toda la serie temporal. El modelo no
``decidió mal'' a partir de una señal nítida; \textbf{no encontró
una señal nítida que examinar}. La diferencia es importante: un
modelo que razona mal sobre evidencia clara puede arreglarse con
más entrenamiento o regularización; un modelo que enfrenta evidencia
ambigua necesita o bien mejor evidencia (más datos, mejor
preprocesamiento) o bien una estrategia distinta.

\textbf{Vías de mejora fuera de alcance.}
Cerrar parcialmente este modo de falla requeriría intervenciones a
nivel de pipeline. Por ejemplo, aplicar filtros más agresivos durante
el preprocesamiento para retirar variaciones estacionales del brillo
estelar (cambios de brillo no relacionados con tránsitos, como
manchas o pulsaciones), o normalizar las curvas por sector
observacional para suavizar diferencias instrumentales entre
sectores TESS. Otra vía sería incrementar el número de ejemplos
etiquetados en el régimen de baja SNR, lo cual depende de que la
NASA continúe el proceso de \textit{vetting} sobre TOIs de
profundidad baja. Ninguna de estas extensiones forma parte del
alcance del presente reporte.

% ============================================================
\section{Conclusiones}
\label{sec:conclusions}

Este trabajo evaluó empíricamente si un modelo selectivo de espacio
de estados (Mamba) puede superar a baselines clásicos en la tarea
de \textit{vetting} de TOIs sobre curvas de luz de TESS, bajo
restricciones realistas de \textit{dataset} y hardware. Sobre
1\,576 TICs etiquetados (CP+FP) y una RTX 3050 de 4\,GB de VRAM,
Mamba \textit{single-view} \textit{locked} alcanzó AUC-ROC $0.763$
en \textit{test} sellado, superando al CNN \textit{single-branch}
inspirado en AstroNet ($0.604$) por $+15.9$ puntos porcentuales bajo
condiciones idénticas. El \textit{ensemble} de cinco semillas elevó
la métrica a $0.806$ con varianza significativamente menor que las
predicciones individuales. El análisis de errores y la
interpretabilidad multi-método coinciden en que los modos de falla
restantes corresponden a curvas de baja SNR, no a errores
sistemáticos del modelo. Como contribución metodológica adicional,
el reporte propone un protocolo \textit{multi-seed} como sustituto
justificado de $K$-fold para arquitecturas costosas en hardware
limitado.

% ============================================================
\bibliographystyle{plain}
\begin{thebibliography}{9}
\bibitem{gu2023mamba} Gu, A. \& Dao, T. (2023).
  \textit{Mamba: Linear-Time Sequence Modeling with Selective State Spaces}.
  arXiv:2312.00752.
\bibitem{shallue2018} Shallue, C. J. \& Vanderburg, A. (2018).
  \textit{Identifying Exoplanets with Deep Learning}. AJ 155(2), 94.
\bibitem{exominer} Valizadegan, H. et al. (2022). \textit{ExoMiner}. ApJ 926(2), 120.
\bibitem{lightkurve} Lightkurve Collaboration (2018). ascl:1812.013.

\bibitem{ricker2015tess}
Ricker, G. R., Winn, J. N., Vanderspek, R., et al. (2015).
\textit{Transiting Exoplanet Survey Satellite (TESS)}.
Journal of Astronomical Telescopes, Instruments, and Systems, 1(1), 014003.

\bibitem{nasaExoplanetArchiveTOI}
NASA Exoplanet Archive. (2026).
\textit{TESS Objects of Interest (TOI) Table Data Column Definitions}.
California Institute of Technology / IPAC.

\bibitem{mastTessProducts}
Mikulski Archive for Space Telescopes (MAST). (2026).
\textit{TESS Archive Documentation: Data Product Overview}.
Space Telescope Science Institute.

\bibitem{tessMission}
NASA. (2026).
\textit{TESS: Transiting Exoplanet Survey Satellite}.
NASA Science Mission Directorate.
\end{thebibliography}

% ============================================================

\end{document}
